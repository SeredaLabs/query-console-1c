# UI-паритет конструктора: корпусная проверка + доработки — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Построить корпусную проверку UI конструктора по фиче-векторам с автоинвариантами и закрыть подтверждённые пробелы паритета/дефекты webview (дубли полей, обрезка «Связи», переименование «Индексы», постоянные ОК/Отмена, клиппинг бокового стрипа).

**Architecture:** Две части одного плана. **Часть A** — новое pure-TS ядро в `tooling/corpus-verify/` (фиче-вектор из `BatchDocument` → классы эквивалентности → выборка представителей → чистые инварианты над DOM-снимком → отчёт покрытия) + Playwright-драйвер, переиспользующий harness описи 7.4. **Часть B** — точечные правки webview-компонентов, каждая с pure-хелпером под vitest либо приёмкой Playwright e2e. Часть A строится первой и служит regression-сетью для Части B.

**Tech Stack:** TypeScript, React (webview), esbuild (бандл), Vitest (pure unit, env=node, только `*.test.ts`), Playwright (e2e/драйвер), `serve` (статический harness).

## Global Constraints

- Pure-логика тестируется Vitest в `test/unit/**/*.test.ts` (env `node`); **нет** jsdom/testing-library — компоненты `.tsx` напрямую не рендерятся в юнитах. Поведение DOM проверяется Playwright (`test/e2e/*.spec.ts`) либо приёмкой драйвера. Это устоявшийся паттерн (7.3/7.4).
- Парс-вход: `parseBatch(text): BatchDocument`; `BatchDocument.members: QueryDocument[]` (операторы пакета); `QueryDocument.members: UnionMember[]` (участники объединения); `UnionMember.model: QueryModel`. Импорт: `import { parseBatch } from 'src/core/query/sdblParser'`, типы — из `src/core/query/batchModel`, `src/core/query/unionModel`, `src/core/query/queryModel`.
- Видимые вкладки (после переименования Task 6): база `['Таблицы и поля','Группировка','Условия','Дополнительно','Объединения/Псевдонимы','Порядок','Итоги','Построитель','Пакет запросов']`; «Связи» вставляется 2-м элементом при >1 таблице активного запроса; «Индексы» — сразу после «Дополнительно» при типе `createTemp`; `dropTemp` → только `['Дополнительно','Пакет запросов']`.
- Корпус — `tmp/query1c/*.txt` (~17 934 файла), один файл = один текст запроса для `parseBatch`. `tmp/` в `.gitignore` — артефакты прогона (скриншоты, отчёт) не коммитятся.
- Намеренные value-add НЕ трогаем (решение ревью 7.5): «Построитель», «Порядок», порядок вкладок, табличная форма «Связи» (на дерево не меняем), боковой стрип, «Обновить кэш», тип «Уничтожение ВТ», меню «Еще ▾».
- Сборка бандла: `npm run build:webview` (→ `out/webview/main.js`). Драйверы собираются esbuild-ом, как `our:inventory`/`real:inventory`.
- Каждая задача завершается коммитом. Сообщения — на русском, как в истории (`feat(phase7.6/7.7): …`), с `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.

---

# Часть A. Корпусная проверка (подфаза 7.6)

## File Structure (Часть A)

- Create `tooling/corpus-verify/features.ts` — `extractFeatures`, `featureKey`, типы `FeatureVector`/`ActiveView` (pure).
- Create `tooling/corpus-verify/classes.ts` — `classifyCorpus`, `sampleRepresentatives` (pure).
- Create `tooling/corpus-verify/invariants.ts` — `expectedTabs`, `checkInvariants`, типы `UiSnapshot`/`Violation` (pure).
- Create `tooling/corpus-verify/report.ts` — `formatCoverageReport` (pure).
- Create `tooling/corpus-verify/snapshot.ts` — `SNAPSHOT_FN` (строка-функция для `page.evaluate`, браузерный код извлечения DOM-снимка).
- Create `tooling/corpus-verify/verify.ts` — Playwright-драйвер (классификация → выборка → прогон → отчёт).
- Create tests `test/unit/corpusFeatures.test.ts`, `corpusClasses.test.ts`, `corpusInvariants.test.ts`, `corpusReport.test.ts`.
- Modify `package.json` — скрипт `corpus:verify`.

---

## Task 1: Фиче-вектор запроса (`features.ts`)

**Files:**
- Create: `tooling/corpus-verify/features.ts`
- Test: `test/unit/corpusFeatures.test.ts`

**Interfaces:**
- Consumes: `parseBatch` (`src/core/query/sdblParser`), `BatchDocument`, `QueryModel`, `JoinKind`, `QueryType`.
- Produces:
  - `interface ActiveView { tableCount: number; queryType: QueryType }`
  - `interface FeatureVector { isPackage: boolean; hasUnions: boolean; maxTables: number; hasJoins: boolean; joinKinds: JoinKind[]; queryTypes: QueryType[]; hasIndexing: boolean; hasGrouping: boolean; hasTotals: boolean; hasOrder: boolean; hasConditions: boolean; hasHaving: boolean; hasSubquery: boolean; hasVirtual: boolean; hasParams: boolean; hasBuilder: boolean; hasTabSections: boolean; hasExpressions: boolean; top: boolean; distinct: boolean; allowed: boolean; active: ActiveView }`
  - `function extractFeatures(batch: BatchDocument): FeatureVector`
  - `function featureKey(fv: FeatureVector): string`

- [ ] **Step 1: Write the failing test**

```ts
// test/unit/corpusFeatures.test.ts
import { describe, it, expect } from 'vitest';
import { parseBatch } from '../../src/core/query/sdblParser';
import { extractFeatures, featureKey } from '../../tooling/corpus-verify/features';

describe('extractFeatures', () => {
  it('single simple select: no joins, one table, queryType select', () => {
    const fv = extractFeatures(parseBatch('ВЫБРАТЬ Валюты.Ссылка ИЗ Справочник.Валюты КАК Валюты'));
    expect(fv.isPackage).toBe(false);
    expect(fv.hasUnions).toBe(false);
    expect(fv.maxTables).toBe(1);
    expect(fv.hasJoins).toBe(false);
    expect(fv.active).toEqual({ tableCount: 1, queryType: 'select' });
  });

  it('detects package (>1 statement) and temp-table create', () => {
    const text = [
      'ВЫБРАТЬ Валюты.Ссылка КАК Ссылка ПОМЕСТИТЬ ВТ ИЗ Справочник.Валюты КАК Валюты',
      'ВЫБРАТЬ ВТ.Ссылка ИЗ ВТ КАК ВТ',
    ].join(';\n');
    const fv = extractFeatures(parseBatch(text));
    expect(fv.isPackage).toBe(true);
    expect(fv.queryTypes).toContain('createTemp');
    expect(fv.active.queryType).toBe('createTemp');
  });

  it('detects union (ОБЪЕДИНИТЬ ВСЕ)', () => {
    const text = 'ВЫБРАТЬ 1 КАК А ОБЪЕДИНИТЬ ВСЕ ВЫБРАТЬ 2 КАК А';
    expect(extractFeatures(parseBatch(text)).hasUnions).toBe(true);
  });

  it('featureKey is stable & order-independent for joinKinds/queryTypes', () => {
    const a = extractFeatures(parseBatch('ВЫБРАТЬ Валюты.Ссылка ИЗ Справочник.Валюты КАК Валюты'));
    const b = extractFeatures(parseBatch('ВЫБРАТЬ Валюты.Код ИЗ Справочник.Валюты КАК Валюты'));
    expect(featureKey(a)).toBe(featureKey(b)); // оба: 1 таблица, select, без секций
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run test/unit/corpusFeatures.test.ts`
Expected: FAIL — `extractFeatures`/`featureKey` не определены (модуль отсутствует).

- [ ] **Step 3: Write minimal implementation**

```ts
// tooling/corpus-verify/features.ts
import type { BatchDocument } from '../../src/core/query/batchModel';
import type { QueryModel, JoinKind, QueryType, Join } from '../../src/core/query/queryModel';

export interface ActiveView {
  tableCount: number;
  queryType: QueryType;
}

export interface FeatureVector {
  isPackage: boolean;
  hasUnions: boolean;
  maxTables: number;
  hasJoins: boolean;
  joinKinds: JoinKind[];
  queryTypes: QueryType[];
  hasIndexing: boolean;
  hasGrouping: boolean;
  hasTotals: boolean;
  hasOrder: boolean;
  hasConditions: boolean;
  hasHaving: boolean;
  hasSubquery: boolean;
  hasVirtual: boolean;
  hasParams: boolean;
  hasBuilder: boolean;
  hasTabSections: boolean;
  hasExpressions: boolean;
  top: boolean;
  distinct: boolean;
  allowed: boolean;
  active: ActiveView;
}

function joinKind(j: Join): JoinKind {
  if (j.leftAll && j.rightAll) return 'full';
  if (j.leftAll || j.rightAll) return 'left'; // правое нормализуется к левому
  return 'inner';
}

function modelHasBuilder(m: QueryModel): boolean {
  const b = m.builder;
  return !!b && (b.fields.length + b.conditions.length + b.order.length + b.totals.length) > 0;
}

export function extractFeatures(batch: BatchDocument): FeatureVector {
  const models: QueryModel[] = [];
  let hasUnions = false;
  for (const doc of batch.members) {
    if (doc.members.length > 1) hasUnions = true;
    for (const um of doc.members) models.push(um.model);
  }
  const joinKinds = new Set<JoinKind>();
  const queryTypes = new Set<QueryType>();
  let maxTables = 0;
  const fv: FeatureVector = {
    isPackage: batch.members.length > 1,
    hasUnions,
    maxTables: 0,
    hasJoins: false,
    joinKinds: [],
    queryTypes: [],
    hasIndexing: false, hasGrouping: false, hasTotals: false, hasOrder: false,
    hasConditions: false, hasHaving: false, hasSubquery: false, hasVirtual: false,
    hasParams: false, hasBuilder: false, hasTabSections: false, hasExpressions: false,
    top: false, distinct: false, allowed: false,
    active: { tableCount: 0, queryType: 'select' },
  };
  for (const m of models) {
    maxTables = Math.max(maxTables, m.tables.length);
    queryTypes.add(m.queryType ?? 'select');
    for (const j of m.joins ?? []) { fv.hasJoins = true; joinKinds.add(joinKind(j)); }
    if ((m.indexing?.indexes.length ?? 0) > 0) fv.hasIndexing = true;
    if (m.grouping && (m.grouping.groupFields.length || m.grouping.aggregates.length || m.grouping.groupSets.length)) fv.hasGrouping = true;
    if (m.totals && (m.totals.groupFields.length || m.totals.totalFields.length)) fv.hasTotals = true;
    if (m.order && (m.order.fields.length || m.order.auto)) fv.hasOrder = true;
    if ((m.conditions?.length ?? 0) > 0) fv.hasConditions = true;
    if ((m.having?.length ?? 0) > 0) fv.hasHaving = true;
    if (m.conditions?.some(c => c.subquery) || m.tables.some(t => t.subquery)) fv.hasSubquery = true;
    if (m.tables.some(t => t.virtual)) fv.hasVirtual = true;
    if (m.conditions?.some(c => c.param)) fv.hasParams = true;
    if (modelHasBuilder(m)) fv.hasBuilder = true;
    if ((m.tabSectionFields?.length ?? 0) > 0) fv.hasTabSections = true;
    if (m.fields.some(f => f.expression)) fv.hasExpressions = true;
    if (m.selection?.top !== undefined) fv.top = true;
    if (m.selection?.distinct) fv.distinct = true;
    if (m.selection?.allowed) fv.allowed = true;
  }
  fv.maxTables = maxTables;
  fv.joinKinds = [...joinKinds].sort();
  fv.queryTypes = [...queryTypes].sort();
  const active = batch.members[0]?.members[0]?.model;
  fv.active = {
    tableCount: active?.tables.length ?? 0,
    queryType: active?.queryType ?? 'select',
  };
  return fv;
}

export function featureKey(fv: FeatureVector): string {
  return JSON.stringify({
    isPackage: fv.isPackage, hasUnions: fv.hasUnions,
    tables: fv.maxTables > 1 ? 'multi' : fv.maxTables, // схлопываем точное число >1
    hasJoins: fv.hasJoins, joinKinds: fv.joinKinds, queryTypes: fv.queryTypes,
    hasIndexing: fv.hasIndexing, hasGrouping: fv.hasGrouping, hasTotals: fv.hasTotals,
    hasOrder: fv.hasOrder, hasConditions: fv.hasConditions, hasHaving: fv.hasHaving,
    hasSubquery: fv.hasSubquery, hasVirtual: fv.hasVirtual, hasParams: fv.hasParams,
    hasBuilder: fv.hasBuilder, hasTabSections: fv.hasTabSections, hasExpressions: fv.hasExpressions,
    top: fv.top, distinct: fv.distinct, allowed: fv.allowed,
  });
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run test/unit/corpusFeatures.test.ts`
Expected: PASS (4 tests).

- [ ] **Step 5: Commit**

```bash
git add tooling/corpus-verify/features.ts test/unit/corpusFeatures.test.ts
git commit -m "feat(phase7.6): фиче-вектор запроса из BatchDocument

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: Классы эквивалентности и выборка (`classes.ts`)

**Files:**
- Create: `tooling/corpus-verify/classes.ts`
- Test: `test/unit/corpusClasses.test.ts`

**Interfaces:**
- Consumes: `FeatureVector`, `featureKey` (Task 1).
- Produces:
  - `interface CorpusEntry { name: string; fv: FeatureVector }`
  - `interface ClassInfo { key: string; members: string[] }`
  - `function classifyCorpus(entries: CorpusEntry[]): ClassInfo[]` — группы по `featureKey`, отсортированы по убыванию размера, имена внутри — лексикографически.
  - `function sampleRepresentatives(classes: ClassInfo[], perClass: number): string[]` — из каждого класса первые `perClass` имён (или все, если меньше); результат отсортирован.

- [ ] **Step 1: Write the failing test**

```ts
// test/unit/corpusClasses.test.ts
import { describe, it, expect } from 'vitest';
import { classifyCorpus, sampleRepresentatives, type CorpusEntry } from '../../tooling/corpus-verify/classes';
import type { FeatureVector } from '../../tooling/corpus-verify/features';

function fv(over: Partial<FeatureVector>): FeatureVector {
  return {
    isPackage: false, hasUnions: false, maxTables: 1, hasJoins: false, joinKinds: [],
    queryTypes: ['select'], hasIndexing: false, hasGrouping: false, hasTotals: false,
    hasOrder: false, hasConditions: false, hasHaving: false, hasSubquery: false,
    hasVirtual: false, hasParams: false, hasBuilder: false, hasTabSections: false,
    hasExpressions: false, top: false, distinct: false, allowed: false,
    active: { tableCount: 1, queryType: 'select' }, ...over,
  };
}

describe('classifyCorpus / sampleRepresentatives', () => {
  it('groups identical feature vectors and sorts by class size desc', () => {
    const entries: CorpusEntry[] = [
      { name: 'a.txt', fv: fv({}) },
      { name: 'b.txt', fv: fv({}) },
      { name: 'c.txt', fv: fv({ hasJoins: true, maxTables: 2 }) },
    ];
    const classes = classifyCorpus(entries);
    expect(classes.length).toBe(2);
    expect(classes[0].members).toEqual(['a.txt', 'b.txt']); // крупнейший класс первым
    expect(classes[1].members).toEqual(['c.txt']);
  });

  it('takes up to perClass representatives from each class', () => {
    const entries: CorpusEntry[] = [
      { name: 'a.txt', fv: fv({}) }, { name: 'b.txt', fv: fv({}) }, { name: 'c.txt', fv: fv({}) },
      { name: 'd.txt', fv: fv({ hasTotals: true }) },
    ];
    const sample = sampleRepresentatives(classifyCorpus(entries), 2);
    expect(sample).toEqual(['a.txt', 'b.txt', 'd.txt']); // 2 из большого класса + 1 редкого
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run test/unit/corpusClasses.test.ts`
Expected: FAIL — модуль `classes.ts` отсутствует.

- [ ] **Step 3: Write minimal implementation**

```ts
// tooling/corpus-verify/classes.ts
import { featureKey, type FeatureVector } from './features';

export interface CorpusEntry { name: string; fv: FeatureVector }
export interface ClassInfo { key: string; members: string[] }

export function classifyCorpus(entries: CorpusEntry[]): ClassInfo[] {
  const map = new Map<string, string[]>();
  for (const e of entries) {
    const k = featureKey(e.fv);
    (map.get(k) ?? map.set(k, []).get(k)!).push(e.name);
  }
  const classes: ClassInfo[] = [...map.entries()].map(([key, members]) => ({
    key, members: [...members].sort(),
  }));
  classes.sort((a, b) => b.members.length - a.members.length || a.key.localeCompare(b.key));
  return classes;
}

export function sampleRepresentatives(classes: ClassInfo[], perClass: number): string[] {
  const picked: string[] = [];
  for (const c of classes) picked.push(...c.members.slice(0, perClass));
  return picked.sort();
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run test/unit/corpusClasses.test.ts`
Expected: PASS (2 tests).

- [ ] **Step 5: Commit**

```bash
git add tooling/corpus-verify/classes.ts test/unit/corpusClasses.test.ts
git commit -m "feat(phase7.6): классы эквивалентности корпуса + выборка представителей

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: Ожидаемые вкладки и инварианты (`invariants.ts`)

**Files:**
- Create: `tooling/corpus-verify/invariants.ts`
- Test: `test/unit/corpusInvariants.test.ts`

**Interfaces:**
- Consumes: `FeatureVector`, `ActiveView` (Task 1); `QueryType`.
- Produces:
  - `function expectedTabs(active: ActiveView): string[]`
  - `interface UiSnapshot { tabs: string[]; tableLabels: string[]; fieldListGroups: { id: string; items: string[] }[]; clipped: { text: string; hasTitle: boolean }[] }`
  - `interface Violation { code: 'TABS' | 'TABLE_COUNT' | 'DUP_FIELDS' | 'CLIP'; detail: string }`
  - `function checkInvariants(snap: UiSnapshot, fv: FeatureVector): Violation[]`

- [ ] **Step 1: Write the failing test**

```ts
// test/unit/corpusInvariants.test.ts
import { describe, it, expect } from 'vitest';
import { expectedTabs, checkInvariants, type UiSnapshot } from '../../tooling/corpus-verify/invariants';
import type { FeatureVector } from '../../tooling/corpus-verify/features';

function fv(active: { tableCount: number; queryType: any }): FeatureVector {
  return {
    isPackage: false, hasUnions: false, maxTables: active.tableCount, hasJoins: active.tableCount > 1,
    joinKinds: [], queryTypes: [active.queryType], hasIndexing: false, hasGrouping: false,
    hasTotals: false, hasOrder: false, hasConditions: false, hasHaving: false, hasSubquery: false,
    hasVirtual: false, hasParams: false, hasBuilder: false, hasTabSections: false, hasExpressions: false,
    top: false, distinct: false, allowed: false, active,
  };
}

describe('expectedTabs', () => {
  it('single table select: base tabs, no Связи/Индексы', () => {
    expect(expectedTabs({ tableCount: 1, queryType: 'select' })).toEqual([
      'Таблицы и поля', 'Группировка', 'Условия', 'Дополнительно',
      'Объединения/Псевдонимы', 'Порядок', 'Итоги', 'Построитель', 'Пакет запросов',
    ]);
  });
  it('multi-table inserts Связи as 2nd', () => {
    expect(expectedTabs({ tableCount: 2, queryType: 'select' })[1]).toBe('Связи');
  });
  it('createTemp inserts Индексы after Дополнительно', () => {
    const tabs = expectedTabs({ tableCount: 1, queryType: 'createTemp' });
    expect(tabs[tabs.indexOf('Дополнительно') + 1]).toBe('Индексы');
  });
  it('dropTemp: only Дополнительно + Пакет запросов', () => {
    expect(expectedTabs({ tableCount: 1, queryType: 'dropTemp' })).toEqual(['Дополнительно', 'Пакет запросов']);
  });
});

describe('checkInvariants', () => {
  const okSnap = (over: Partial<UiSnapshot> = {}): UiSnapshot => ({
    tabs: expectedTabs({ tableCount: 1, queryType: 'select' }),
    tableLabels: ['Валюты'],
    fieldListGroups: [{ id: 'order-source', items: ['Валюты.Код', 'Валюты.Наименование'] }],
    clipped: [],
    ...over,
  });

  it('clean snapshot → no violations', () => {
    expect(checkInvariants(okSnap(), fv({ tableCount: 1, queryType: 'select' }))).toEqual([]);
  });
  it('flags tab-set mismatch', () => {
    const v = checkInvariants(okSnap({ tabs: ['Таблицы и поля'] }), fv({ tableCount: 1, queryType: 'select' }));
    expect(v.some(x => x.code === 'TABS')).toBe(true);
  });
  it('flags duplicate field nodes', () => {
    const v = checkInvariants(okSnap({ fieldListGroups: [{ id: 'order-source', items: ['Валюты.Дата', 'Валюты.Дата'] }] }), fv({ tableCount: 1, queryType: 'select' }));
    expect(v.some(x => x.code === 'DUP_FIELDS')).toBe(true);
  });
  it('flags clipped text without title', () => {
    const v = checkInvariants(okSnap({ clipped: [{ text: 'ОченьДлинноеИмя', hasTitle: false }] }), fv({ tableCount: 1, queryType: 'select' }));
    expect(v.some(x => x.code === 'CLIP')).toBe(true);
  });
  it('clipped WITH title → no CLIP violation', () => {
    const v = checkInvariants(okSnap({ clipped: [{ text: 'ОченьДлинноеИмя', hasTitle: true }] }), fv({ tableCount: 1, queryType: 'select' }));
    expect(v.some(x => x.code === 'CLIP')).toBe(false);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run test/unit/corpusInvariants.test.ts`
Expected: FAIL — модуль `invariants.ts` отсутствует.

- [ ] **Step 3: Write minimal implementation**

```ts
// tooling/corpus-verify/invariants.ts
import type { FeatureVector, ActiveView } from './features';

const BASE_TABS = [
  'Таблицы и поля', 'Группировка', 'Условия', 'Дополнительно',
  'Объединения/Псевдонимы', 'Порядок', 'Итоги', 'Построитель', 'Пакет запросов',
];

export function expectedTabs(active: ActiveView): string[] {
  if (active.queryType === 'dropTemp') return ['Дополнительно', 'Пакет запросов'];
  let tabs = active.tableCount > 1 ? [BASE_TABS[0], 'Связи', ...BASE_TABS.slice(1)] : [...BASE_TABS];
  if (active.queryType === 'createTemp') {
    const i = tabs.indexOf('Дополнительно');
    tabs = [...tabs.slice(0, i + 1), 'Индексы', ...tabs.slice(i + 1)];
  }
  return tabs;
}

export interface UiSnapshot {
  tabs: string[];
  tableLabels: string[];
  fieldListGroups: { id: string; items: string[] }[];
  clipped: { text: string; hasTitle: boolean }[];
}

export interface Violation {
  code: 'TABS' | 'TABLE_COUNT' | 'DUP_FIELDS' | 'CLIP';
  detail: string;
}

export function checkInvariants(snap: UiSnapshot, fv: FeatureVector): Violation[] {
  const out: Violation[] = [];
  const exp = expectedTabs(fv.active);
  if (JSON.stringify(snap.tabs) !== JSON.stringify(exp)) {
    out.push({ code: 'TABS', detail: `ожидалось [${exp.join(', ')}], получено [${snap.tabs.join(', ')}]` });
  }
  if (fv.active.queryType !== 'dropTemp' && snap.tableLabels.length !== fv.active.tableCount) {
    out.push({ code: 'TABLE_COUNT', detail: `таблиц в панели ${snap.tableLabels.length}, в модели ${fv.active.tableCount}` });
  }
  for (const g of snap.fieldListGroups) {
    const seen = new Set<string>();
    for (const item of g.items) {
      if (seen.has(item)) { out.push({ code: 'DUP_FIELDS', detail: `${g.id}: дубль «${item}»` }); }
      seen.add(item);
    }
  }
  for (const c of snap.clipped) {
    if (!c.hasTitle) out.push({ code: 'CLIP', detail: `обрезка без тултипа: «${c.text}»` });
  }
  return out;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run test/unit/corpusInvariants.test.ts`
Expected: PASS (9 tests).

- [ ] **Step 5: Commit**

```bash
git add tooling/corpus-verify/invariants.ts test/unit/corpusInvariants.test.ts
git commit -m "feat(phase7.6): ожидаемые вкладки + чистые UI-инварианты

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 4: Отчёт покрытия (`report.ts`)

**Files:**
- Create: `tooling/corpus-verify/report.ts`
- Test: `test/unit/corpusReport.test.ts`

**Interfaces:**
- Consumes: `ClassInfo` (Task 2), `Violation` (Task 3).
- Produces:
  - `interface QueryResult { name: string; key: string; violations: Violation[] }`
  - `function formatCoverageReport(input: { classes: ClassInfo[]; sampled: string[]; results: QueryResult[] }): string` — Markdown: всего классов / покрыто выборкой / запросов прогнано / провалов по кодам + список провалов с именами файлов.

- [ ] **Step 1: Write the failing test**

```ts
// test/unit/corpusReport.test.ts
import { describe, it, expect } from 'vitest';
import { formatCoverageReport } from '../../tooling/corpus-verify/report';

describe('formatCoverageReport', () => {
  it('summarises classes, coverage and violations by code', () => {
    const md = formatCoverageReport({
      classes: [{ key: 'k1', members: ['a.txt', 'b.txt'] }, { key: 'k2', members: ['c.txt'] }],
      sampled: ['a.txt', 'c.txt'],
      results: [
        { name: 'a.txt', key: 'k1', violations: [] },
        { name: 'c.txt', key: 'k2', violations: [{ code: 'DUP_FIELDS', detail: 'order-source: дубль «X»' }] },
      ],
    });
    expect(md).toContain('Классов: 2');
    expect(md).toContain('Покрыто выборкой: 2');
    expect(md).toContain('Прогнано: 2');
    expect(md).toContain('DUP_FIELDS: 1');
    expect(md).toContain('c.txt');
  });

  it('flags uncovered classes (no sampled member)', () => {
    const md = formatCoverageReport({
      classes: [{ key: 'k1', members: ['a.txt'] }, { key: 'k2', members: ['b.txt'] }],
      sampled: ['a.txt'],
      results: [{ name: 'a.txt', key: 'k1', violations: [] }],
    });
    expect(md).toContain('Непокрытых классов: 1');
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run test/unit/corpusReport.test.ts`
Expected: FAIL — модуль `report.ts` отсутствует.

- [ ] **Step 3: Write minimal implementation**

```ts
// tooling/corpus-verify/report.ts
import type { ClassInfo } from './classes';
import type { Violation } from './invariants';

export interface QueryResult { name: string; key: string; violations: Violation[] }

export function formatCoverageReport(input: {
  classes: ClassInfo[]; sampled: string[]; results: QueryResult[];
}): string {
  const { classes, sampled, results } = input;
  const sampledSet = new Set(sampled);
  const coveredKeys = new Set(classes.filter(c => c.members.some(m => sampledSet.has(m))).map(c => c.key));
  const uncovered = classes.filter(c => !coveredKeys.has(c.key));

  const byCode = new Map<string, number>();
  const failed = results.filter(r => r.violations.length > 0);
  for (const r of failed) for (const v of r.violations) byCode.set(v.code, (byCode.get(v.code) ?? 0) + 1);

  const lines: string[] = [];
  lines.push('# Отчёт корпусной проверки UI', '');
  lines.push(`- Классов: ${classes.length}`);
  lines.push(`- Покрыто выборкой: ${coveredKeys.size}`);
  lines.push(`- Непокрытых классов: ${uncovered.length}`);
  lines.push(`- Прогнано: ${results.length}`);
  lines.push(`- Провалов (запросов): ${failed.length}`, '');
  lines.push('## Провалы по типам');
  if (byCode.size === 0) lines.push('- нет');
  else for (const [code, n] of [...byCode.entries()].sort()) lines.push(`- ${code}: ${n}`);
  lines.push('', '## Проваленные запросы');
  if (failed.length === 0) lines.push('- нет');
  else for (const r of failed) lines.push(`- \`${r.name}\` — ${r.violations.map(v => `${v.code}: ${v.detail}`).join('; ')}`);
  if (uncovered.length > 0) {
    lines.push('', '## Непокрытые классы (нет представителя в выборке)');
    for (const c of uncovered) lines.push(`- ${c.key} (${c.members.length} запр.)`);
  }
  return lines.join('\n') + '\n';
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run test/unit/corpusReport.test.ts`
Expected: PASS (2 tests).

- [ ] **Step 5: Commit**

```bash
git add tooling/corpus-verify/report.ts test/unit/corpusReport.test.ts
git commit -m "feat(phase7.6): форматирование отчёта покрытия

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 5: DOM-снимок + драйвер + скрипт (`snapshot.ts`, `verify.ts`)

**Files:**
- Create: `tooling/corpus-verify/snapshot.ts`
- Create: `tooling/corpus-verify/verify.ts`
- Modify: `package.json` (скрипт `corpus:verify`)

**Interfaces:**
- Consumes: `parseBatch`; `extractFeatures` (T1); `classifyCorpus`/`sampleRepresentatives` (T2); `checkInvariants`/`UiSnapshot` (T3); `formatCoverageReport`/`QueryResult` (T4); harness описи `tooling/our-constructor/harness/` + `saveScreenshot` из `tooling/real-constructor/screenshot.ts`.
- Produces: исполняемый драйвер; `SNAPSHOT_FN: string` — тело браузерной функции для `page.evaluate`, собирающей `UiSnapshot` из DOM (`[data-testid="tabsbar"] [data-tab]`, `[data-table-id]`, списки полей по `data-field-source`, обрезанные `span` где `scrollWidth>clientWidth`).

Примечание: для атрибута `data-field-source` на исходных списках полей см. Task 7 (хук добавляется там же, где чинится дедуп). До Task 7 группа полей в снимке будет пустой — это не мешает прогону, инвариант DUP_FIELDS просто не сработает.

- [ ] **Step 1: Написать `snapshot.ts`** (браузерный код как строка — выполняется в `page.evaluate`)

```ts
// tooling/corpus-verify/snapshot.ts
// Тело функции, исполняемой в браузере через page.evaluate(SNAPSHOT_FN).
// Возвращает UiSnapshot (структура из invariants.ts), сериализуемый в Node.
export const SNAPSHOT_FN = `() => {
  const tabs = Array.from(document.querySelectorAll('[data-testid="tabsbar"] [data-tab]'))
    .map(e => e.getAttribute('data-tab') || '');
  const tableLabels = Array.from(document.querySelectorAll('[data-table-id]'))
    .map(e => (e.textContent || '').trim());
  const groups = {};
  Array.from(document.querySelectorAll('[data-field-source]')).forEach(el => {
    const id = el.getAttribute('data-field-source') || 'unknown';
    const items = Array.from(el.querySelectorAll('[data-field-item]')).map(i => (i.textContent || '').trim());
    groups[id] = (groups[id] || []).concat(items);
  });
  const fieldListGroups = Object.keys(groups).map(id => ({ id, items: groups[id] }));
  const clipped = [];
  Array.from(document.querySelectorAll('span')).forEach(s => {
    if (s.scrollWidth > s.clientWidth + 1 && (s.textContent || '').trim()) {
      clipped.push({ text: (s.textContent || '').trim(), hasTitle: !!s.getAttribute('title') });
    }
  });
  return { tabs, tableLabels, fieldListGroups, clipped };
}`;
```

- [ ] **Step 2: Написать драйвер `verify.ts`**

```ts
// tooling/corpus-verify/verify.ts
import * as fs from 'fs';
import * as path from 'path';
import { chromium, type Browser } from '@playwright/test';
import { parseBatch } from '../../src/core/query/sdblParser';
import { extractFeatures } from './features';
import { classifyCorpus, sampleRepresentatives, type CorpusEntry } from './classes';
import { checkInvariants, type UiSnapshot } from './invariants';
import { formatCoverageReport, type QueryResult } from './report';
import { SNAPSHOT_FN } from './snapshot';
import { saveScreenshot } from '../real-constructor/screenshot';

const CORPUS_DIR = path.resolve('tmp/query1c');
const OUT_DIR = path.resolve('tmp/phase7.6-corpus-verify');
const PER_CLASS = Number(process.env.PER_CLASS ?? 2);
const PORT = 5599;

async function startServer(): Promise<{ close: () => void }> {
  const { spawn } = await import('child_process');
  const proc = spawn('npx', ['serve', 'tooling/our-constructor/harness', '-p', String(PORT), '--no-port-switching'], { stdio: 'ignore' });
  await new Promise(r => setTimeout(r, 1500));
  return { close: () => proc.kill() };
}

async function main() {
  fs.mkdirSync(OUT_DIR, { recursive: true });
  // 1. Классификация всего корпуса (pure, без браузера).
  const files = fs.readdirSync(CORPUS_DIR).filter(f => f.endsWith('.txt')).sort();
  const entries: CorpusEntry[] = [];
  const parseFailures: string[] = [];
  for (const name of files) {
    try {
      const fv = extractFeatures(parseBatch(fs.readFileSync(path.join(CORPUS_DIR, name), 'utf8')));
      entries.push({ name, fv });
    } catch (e) {
      parseFailures.push(`${name}: ${(e as Error).message}`);
    }
  }
  const classes = classifyCorpus(entries);
  const sampled = sampleRepresentatives(classes, PER_CLASS);
  const fvByName = new Map(entries.map(e => [e.name, e.fv]));
  console.log(`Корпус: ${files.length}, классов: ${classes.length}, выборка: ${sampled.length}, parse-fail: ${parseFailures.length}`);

  // 2. Прогон выборки через webview (DOM-снимок → инварианты, скриншот только на провал).
  const server = await startServer();
  let browser: Browser | undefined;
  const results: QueryResult[] = [];
  try {
    browser = await chromium.launch({ headless: true });
    const ctx = await browser.newContext({ viewport: { width: 1600, height: 1000 } });
    const page = await ctx.newPage();
    page.on('pageerror', e => results.push({ name: '(pageerror)', key: '', violations: [{ code: 'TABS', detail: `pageerror: ${e.message}` }] }));
    await page.goto(`http://localhost:${PORT}/index.html`, { waitUntil: 'domcontentloaded' });
    await page.waitForFunction(() => (window as any).__metaApplied === true, undefined, { timeout: 60_000 });
    for (const name of sampled) {
      const text = fs.readFileSync(path.join(CORPUS_DIR, name), 'utf8');
      const fv = fvByName.get(name)!;
      try {
        await page.evaluate(t => (window as any).__inv.loadQuery(t), text);
        await page.locator('[data-testid="tabsbar"] [data-tab]').first().waitFor({ timeout: 30_000 });
        await page.waitForTimeout(400);
        const snap = (await page.evaluate(SNAPSHOT_FN as any)) as UiSnapshot;
        const violations = checkInvariants(snap, fv);
        results.push({ name, key: '', violations });
        if (violations.length > 0) {
          await saveScreenshot(page, path.join(OUT_DIR, name.replace(/\.[^.]+$/, '')), 0, 'violation');
        }
      } catch (e) {
        results.push({ name, key: '', violations: [{ code: 'TABS', detail: `прогон упал: ${(e as Error).message}` }] });
      }
    }
  } finally {
    await browser?.close();
    server.close();
  }

  // 3. Отчёт.
  const report = formatCoverageReport({ classes, sampled, results })
    + (parseFailures.length ? `\n## Parse-фейлы (${parseFailures.length})\n` + parseFailures.slice(0, 50).map(s => `- ${s}`).join('\n') + '\n' : '');
  fs.writeFileSync(path.join(OUT_DIR, 'REPORT.md'), report);
  console.log(`Отчёт: ${path.join(OUT_DIR, 'REPORT.md')}`);
}

main().catch(e => { console.error(e); process.exit(1); });
```

- [ ] **Step 3: Добавить скрипт в `package.json`** (рядом с `our:inventory`, строка 73)

```json
    "corpus:verify": "npm run build:webview && cp tmp/parser_data/model-cache.json tooling/our-constructor/harness/metadata.json && cp out/webview/main.js tooling/our-constructor/harness/main.js && esbuild tooling/corpus-verify/verify.ts --bundle --outfile=out/corpus-verify/verify.js --platform=node --format=cjs --packages=external && node out/corpus-verify/verify.js",
```

> Точную форму копирования бандла/метаданных в harness сверить с тем, как это делает скрипт `our:inventory` (Task выполняется после чтения его реализации `tooling/our-constructor/inventory.ts`); привести в соответствие, чтобы harness получал тот же `main.js` и `metadata.json`.

- [ ] **Step 4: Acceptance — пробный прогон на маленькой выборке**

Run: `PER_CLASS=1 npm run corpus:verify`
Expected: лог `Корпус: <N>, классов: <K>, выборка: <S>`; создан `tmp/phase7.6-corpus-verify/REPORT.md` с разделами «Классов/Покрыто/Прогнано/Провалов». Браузер не падает на старте. (Полный прогон и триаж — Task 11.)

- [ ] **Step 5: Commit** (артефакты `tmp/` не коммитятся — в `.gitignore`)

```bash
git add tooling/corpus-verify/snapshot.ts tooling/corpus-verify/verify.ts package.json
git commit -m "feat(phase7.6): драйвер корпусной проверки + DOM-снимок + скрипт corpus:verify

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

# Часть B. Доработки webview (подфаза 7.7)

## File Structure (Часть B)

- Modify `src/webview/components/TabsBar.tsx` — `TABS`: «Индекс» → «Индексы» (Task 6).
- Modify `src/webview/App.tsx` — все строковые сравнения вкладки «Индекс» → «Индексы»; ОК/Отмена в нижней панели (Task 9); overflow бокового стрипа (Task 10).
- Create `src/webview/fieldSource.ts` — `distinctFieldRefs` (pure, Task 7).
- Modify `src/webview/components/OrderTab.tsx` — дедуп исходного списка + хуки `data-field-source`/`data-field-item` (Task 7).
- Modify `src/webview/components/ConnectionsTab.tsx` — `title` на селектах таблиц + ширина (Task 8).
- Modify `test/e2e/webview.spec.ts` — приёмочные проверки (Tasks 8–10).

---

## Task 6: Переименование вкладки «Индекс» → «Индексы»

**Files:**
- Modify: `src/webview/components/TabsBar.tsx:3`
- Modify: `src/webview/App.tsx` (строки 140–141, 144, 151, 153, 155, 165, 167, 169, 383, 467 — все вхождения литерала `'Индекс'` как идентификатора вкладки и комментарии)
- Test: `test/unit/tabsBar.test.ts`

**Interfaces:**
- Produces: `TABS` содержит `'Индексы'` (не `'Индекс'`); активная вкладка/видимость индекса по строке `'Индексы'`.

- [ ] **Step 1: Write the failing test**

```ts
// test/unit/tabsBar.test.ts
import { describe, it, expect } from 'vitest';
import { TABS } from '../../src/webview/components/TabsBar';

describe('TABS', () => {
  it('uses plural «Индексы» (parity with real constructor), not «Индекс»', () => {
    expect(TABS).toContain('Индексы');
    expect(TABS).not.toContain('Индекс');
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run test/unit/tabsBar.test.ts`
Expected: FAIL — `TABS` содержит `'Индекс'`.

- [ ] **Step 3: Реализация**

В `src/webview/components/TabsBar.tsx` строка 3 — заменить в массиве `'Индекс'` на `'Индексы'`.

В `src/webview/App.tsx` заменить КАЖДЫЙ строковый литерал `'Индекс'` (идентификатор вкладки) на `'Индексы'`:
- стр. 144: `const baseTabs = TABS.filter(t => t !== 'Индексы');`
- стр. 153: `if (showIndexTab && finalTabs.includes('Дополнительно') && !finalTabs.includes('Индексы')) {`
- стр. 155: `finalTabs = [...finalTabs.slice(0, i + 1), 'Индексы', ...finalTabs.slice(i + 1)];`
- стр. 167: `if (!showIndexTab && activeTab === 'Индексы') {`
- стр. 383: `{activeTab === 'Индексы' && (`
- стр. 467: `… && activeTab !== 'Индексы' && …`
- комментарии (стр. 140–141, 151, 165) — поправить упоминание названия на «Индексы» для согласованности.

Проверка полноты: `grep -n "'Индекс'" src/webview/App.tsx src/webview/components/TabsBar.tsx` должен вернуть пусто.

- [ ] **Step 4: Run tests + сборка**

Run: `npx vitest run test/unit/tabsBar.test.ts && npm run build:webview`
Expected: тест PASS; бандл собирается без ошибок TypeScript.

- [ ] **Step 5: Commit**

```bash
git add src/webview/components/TabsBar.tsx src/webview/App.tsx test/unit/tabsBar.test.ts
git commit -m "feat(phase7.7): вкладка «Индекс» → «Индексы» (паритет с реальным)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 7: Дедуп исходного списка полей (E2) + хуки снимка

**Files:**
- Create: `src/webview/fieldSource.ts`
- Modify: `src/webview/components/OrderTab.tsx` (строки 50, 95–114)
- Test: `test/unit/fieldSource.test.ts`

**Interfaces:**
- Consumes: `SelectedField` (queryModel).
- Produces: `function distinctFieldRefs<T extends { tableId: string; path?: string; expression?: string }>(fields: T[]): T[]` — оставляет простые поля (есть `path`, нет `expression`), убирая повторы по ключу `tableId:path` с сохранением порядка первого появления.

- [ ] **Step 1: Write the failing test**

```ts
// test/unit/fieldSource.test.ts
import { describe, it, expect } from 'vitest';
import { distinctFieldRefs } from '../../src/webview/fieldSource';

describe('distinctFieldRefs', () => {
  it('removes duplicate (tableId, path) keeping first order; drops expressions', () => {
    const out = distinctFieldRefs([
      { tableId: 't1', path: 'Дата', alias: 'Дата' },
      { tableId: 't1', path: 'Ссылка' },
      { tableId: 't1', path: 'Дата', alias: 'ДатаЗаказа' }, // дубль (tableId, path)
      { tableId: 't1', expression: 'ГОД(Т.Дата)' },         // выражение — исключается
    ] as any);
    expect(out.map((f: any) => f.path)).toEqual(['Дата', 'Ссылка']);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run test/unit/fieldSource.test.ts`
Expected: FAIL — модуль `fieldSource.ts` отсутствует.

- [ ] **Step 3: Реализация хелпера**

```ts
// src/webview/fieldSource.ts
/**
 * Уникальные простые поля выборки для исходных списков «Поля» (Порядок/Итоги/…).
 * Несколько колонок выборки могут делить (tableId, path) под разными псевдонимами
 * (`Т.Дата КАК Дата`, `Т.Дата КАК ДатаЗаказа`) — в списке-источнике, который
 * адресует поля по (tableId, path), это давало визуальный дубль (фаза 7.5, E2).
 */
export function distinctFieldRefs<T extends { tableId: string; path?: string; expression?: string }>(fields: T[]): T[] {
  const seen = new Set<string>();
  const out: T[] = [];
  for (const f of fields) {
    if (f.expression || !f.path) continue;
    const key = `${f.tableId}:${f.path}`;
    if (seen.has(key)) continue;
    seen.add(key);
    out.push(f);
  }
  return out;
}
```

- [ ] **Step 4: Применить в `OrderTab.tsx`**

Импорт (после строки 3):
```ts
import { distinctFieldRefs } from '../fieldSource';
```
Строка 50 — заменить:
```ts
  const sourceFields = distinctFieldRefs(selectedFields);
```
Хуки для DOM-снимка корпусной проверки — обернуть левый список «Поля». Заменить контейнер `dropZone` левого списка (строки 97 и его элементы 98–107) так, чтобы корневой `div` нёс `data-field-source="order-source"`, а каждая строка — `data-field-item`:
```tsx
          <div style={dropZone} data-field-source="order-source">
            {sourceFields.map((f, i) => (
              <div
                key={`${f.tableId}:${f.path}:${i}`}
                data-field-item
                draggable
                onDragStart={e => dragStart(e, f.tableId, f.path!)}
                style={{ ...ROW, cursor: 'grab', justifyContent: 'flex-start' }}
              >
                <span>{labelFor(f.tableId, f.path!)}</span>
              </div>
            ))}
```
(`f.path!` — после `distinctFieldRefs` у элементов `path` гарантированно задан.)

- [ ] **Step 5: Run tests + сборка + commit**

Run: `npx vitest run test/unit/fieldSource.test.ts && npm run build:webview`
Expected: PASS; бандл собирается.

```bash
git add src/webview/fieldSource.ts src/webview/components/OrderTab.tsx test/unit/fieldSource.test.ts
git commit -m "fix(phase7.7): дедуп исходного списка полей «Порядок» (E2) + data-хуки снимка

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 8: Обрезка имён таблиц в «Связи» (E1)

**Files:**
- Modify: `src/webview/components/ConnectionsTab.tsx` (строки 104, 134–144)
- Test: `test/e2e/webview.spec.ts` (новый кейс)

**Interfaces:** без новых экспортируемых символов; селект таблицы получает `title` с полным псевдонимом, `W_TABLE` увеличен.

- [ ] **Step 1: Написать приёмочный e2e (падающий)**

Добавить в конец `test/describe` в `test/e2e/webview.spec.ts`:
```ts
  test('Связи: селект таблицы имеет title с полным псевдонимом (anti-clip)', async ({ page }) => {
    await page.goto(BASE);
    // Добавляем две таблицы, чтобы появилась вкладка «Связи».
    for (const name of ['Справочник.Валюты', 'Справочник.Валюты']) {
      await page.locator('text=Справочники').click();
      await page.locator(`[data-table-fullname="${name}"]`).click();
      await page.locator('button[title="Добавить таблицу"]').click();
    }
    await page.locator('[data-testid="tabsbar"] [data-tab="Связи"]').click();
    await page.locator('button[title="Добавить связь"]').click();
    const sel = page.locator('select[title]').first();
    await expect(sel).toHaveAttribute('title', /.+/); // непустой тултип полного имени
  });
```

- [ ] **Step 2: Запустить — убедиться, что падает**

Run: `npm run build:webview && npx playwright test -g "anti-clip"`
Expected: FAIL — у селекта нет атрибута `title`.

- [ ] **Step 3: Реализация**

`ConnectionsTab.tsx` строка 104: `const W_TABLE = 180;` (было 150).
В `tableSelect` (строки 134–144) добавить `title` = псевдоним выбранной таблицы:
```tsx
  const tableSelect = (index: number, condIndex: number, side: 'left' | 'right', value: string) => {
    const sel = selectedTables.find(t => t.id === value);
    const label = sel ? defaultTableAlias(sel) : '';
    return (
      <select
        value={value}
        title={label}
        onChange={e => onSetTable(index, side, e.target.value, condIndex)}
        style={{ ...INPUT, width: W_TABLE, flexShrink: 0 }}
      >
        {selectedTables.map(t => (
          <option key={t.id} value={t.id}>{defaultTableAlias(t)}</option>
        ))}
      </select>
    );
  };
```

- [ ] **Step 4: Запустить e2e — PASS**

Run: `npm run build:webview && npx playwright test -g "anti-clip"`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/webview/components/ConnectionsTab.tsx test/e2e/webview.spec.ts
git commit -m "fix(phase7.7): тултип полного имени таблицы в «Связи» + ширина (E1)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 9: Постоянные «ОК»/«Отмена» в нижней панели

**Files:**
- Modify: `src/webview/App.tsx` (нижняя панель, строки 476–479)
- Test: `test/e2e/webview.spec.ts` (новый кейс)

**Interfaces:** нижняя панель на всех вкладках содержит кнопки «Запрос», «ОК» (→ `insertText`), «Отмена» (→ `cancel`). Используются существующие `handleInsert`, `handleCancel`, `batchText`.

- [ ] **Step 1: Написать падающий e2e**

```ts
  test('нижняя панель: ОК постит insertText, Отмена постит cancel', async ({ page }) => {
    await page.goto(BASE);
    await expect(page.locator('button:has-text("Запрос")')).toBeVisible();
    await page.locator('button:has-text("ОК")').click();
    await page.locator('button:has-text("Отмена")').click();
    const types = await page.evaluate(() => (window as any).__webviewMessages.map((m: any) => m.type));
    expect(types).toContain('insertText');
    expect(types).toContain('cancel');
  });
```

- [ ] **Step 2: Запустить — FAIL**

Run: `npm run build:webview && npx playwright test -g "нижняя панель"`
Expected: FAIL — кнопок «ОК»/«Отмена» в нижней панели нет.

- [ ] **Step 3: Реализация** — заменить нижнюю панель (строки 476–479):

```tsx
      {/* Bottom bar */}
      <div style={{ display: 'flex', alignItems: 'center', gap: 8, padding: '4px 8px', borderTop: '1px solid var(--vscode-panel-border, #444)' }}>
        <button style={BTN} onClick={handleShowQuery}>Запрос</button>
        <div style={{ flex: 1 }} />
        <button style={BTN} onClick={() => handleInsert(batchText)}>ОК</button>
        <button style={BTN} onClick={handleCancel}>Отмена</button>
      </div>
```

- [ ] **Step 4: Запустить — PASS**

Run: `npm run build:webview && npx playwright test -g "нижняя панель"`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/webview/App.tsx test/e2e/webview.spec.ts
git commit -m "feat(phase7.7): постоянные ОК/Отмена в нижней панели конструктора

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 10: Скролл/анти-клиппинг бокового стрипа пакета (E3)

**Files:**
- Modify: `src/webview/App.tsx` (контейнер `sideTabsStrip`, строки 182–208)
- Test: `test/e2e/webview.spec.ts` (новый кейс — проверка стилей контейнера)

**Interfaces:** контейнер стрипа получает `overflowY: 'auto'`, `maxHeight: '100%'`; элементы — `flexShrink: 0`. (Тултип `title={name}` уже есть, строка 191.)

- [ ] **Step 1: Написать падающий e2e** (проверяем вычисленный стиль контейнера; стрип присутствует в DOM как `div` с `writingMode: vertical-rl` у детей — помечаем контейнер `data-testid`)

Сначала добавить хук: в `sideTabsStrip` корневому `div` (строка 183) добавить `data-testid="side-strip"`. Затем тест:
```ts
  test('боковой стрип пакета: контейнер скроллится по вертикали', async ({ page }) => {
    await page.goto(BASE);
    // Стрип появляется только при >1 запросе пакета; здесь проверяем, что КОГДА он есть,
    // у него overflowY=auto. Если стрипа нет в стартовом состоянии — кейс пропускаем.
    const strip = page.locator('[data-testid="side-strip"]');
    if (await strip.count() === 0) test.skip(true, 'стрип не виден без пакета');
    const overflow = await strip.evaluate(el => getComputedStyle(el).overflowY);
    expect(overflow).toBe('auto');
  });
```

> Прим.: стартовое состояние harness — один запрос, стрип скрыт (`showSideTabs` ложно). Кейс служит регрессионным стражем стиля и не падает ложно (skip при отсутствии стрипа). Фактическая проверка клиппинга на пакетных запросах — через `corpus:verify` (инвариант CLIP) в Task 11.

- [ ] **Step 2: Запустить — кейс проходит как skip ИЛИ падает, если стрип виден без overflow**

Run: `npm run build:webview && npx playwright test -g "боковой стрип"`
Expected: SKIP (стрип не виден в стартовом состоянии) — это нормальный старт; цель шага — закрепить хук и стиль.

- [ ] **Step 3: Реализация** — в `sideTabsStrip` (строка 183) контейнер:

```tsx
    <div data-testid="side-strip" style={{ display: 'flex', flexDirection: 'column', overflowY: 'auto', maxHeight: '100%', borderLeft: '1px solid var(--vscode-panel-border, #444)', background: 'var(--vscode-editorGroupHeader-tabsBackground, #252526)' }}>
```
и каждому элементу (объект стиля начиная со строки 192) добавить `flexShrink: 0,`.

- [ ] **Step 4: Сборка + e2e**

Run: `npm run build:webview && npx playwright test -g "боковой стрип"`
Expected: PASS/SKIP без ошибок; бандл собирается.

- [ ] **Step 5: Commit**

```bash
git add src/webview/App.tsx test/e2e/webview.spec.ts
git commit -m "fix(phase7.7): скролл бокового стрипа пакета, анти-клиппинг (E3)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 11: Полный корпусной прогон, триаж и закрытие подфаз

**Files:**
- Modify: `docs/PHASE_7.md` (разделы «Подфаза 5/6/7»)
- (артефакты `tmp/phase7.6-corpus-verify/REPORT.md` — не коммитятся)

- [ ] **Step 1: Полный прогон корпусной проверки**

Run: `npm run corpus:verify` (без `PER_CLASS` → дефолт 2)
Expected: создан `tmp/phase7.6-corpus-verify/REPORT.md`; в логе число классов и выборки; прогон завершается без падения процесса.

- [ ] **Step 2: Триаж отчёта**

Прочитать `REPORT.md`. Для каждого провала по кодам:
- `DUP_FIELDS` вне `order-source` → значит дубль есть и в других списках (Группировка/Итоги/Индексы): применить `distinctFieldRefs` и `data-field-source`/`data-field-item` к соответствующему компоненту тем же приёмом, что в Task 7 (по одному компоненту = по одному коммиту `fix(phase7.7): …`).
- `CLIP` → довести анти-клиппинг (тултип/ширина/overflow) в указанном компоненте.
- `TABS`/`TABLE_COUNT` → расследовать как баг видимости/полноты (диагностика, отдельный коммит-фикс).
- `parse-фейлы` → зафиксировать список в разделе PHASE_7 как известное ограничение обратного парсера (вход в будущую фазу), не чинить здесь.

Зафиксировать, что **непокрытых классов = 0** (иначе поднять `PER_CLASS` или добить редкие классы и перегнать).

- [ ] **Step 3: Перепрогон после фиксов**

Run: `npx vitest run && npm run corpus:verify`
Expected: все юниты зелёные; в `REPORT.md` провалов по `DUP_FIELDS`/`CLIP` нет (или явно объяснены и вынесены).

- [ ] **Step 4: Закрыть подфазы в `docs/PHASE_7.md`**

Дописать после «Подфаза 4» три раздела (стиль 7.1–7.4): «Подфаза 5» (анализ паритета — ссылка на спек), «Подфаза 6» (корпусная проверка — что построено, ссылки на план/спек, итог `test:unit`), «Подфаза 7» (доработки UI — список закрытых E1/E2/«Индексы»/ОК-Отмена/стрип + результат прогона). Указать, что `REPORT.md` и скриншоты — регенерируемые артефакты в `tmp/`.

- [ ] **Step 5: Commit**

```bash
git add docs/PHASE_7.md
git commit -m "docs(phase7.5-7.7): закрытие — анализ паритета, корпусная проверка, доработки UI

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Self-Review

**Spec coverage:**
- §1 Дифф 7.3↔7.4 — отражён в `expectedTabs`/`checkInvariants` (формализует ожидаемый набор/порядок вкладок) и в спеке (документ Подфазы 5, Task 11 ссылается).
- §2 Доработки: «Индексы» → Task 6; ОК/Отмена → Task 9. (Остальные §2-пункты ревью исключило — value-add.)
- §3 Каталог ошибок: E1 → Task 8; E2 → Task 7 (+ распространение в Task 11); E3 → Task 10. Систематические классы D-* → инварианты Task 3, прогон Task 5/11.
- §4 Методология (фиче-вектор → классы → выборка → инварианты → отчёт) → Tasks 1–5; полный прогон + карта покрытия/непокрытых классов → Task 11.
- Бэклог B1→Task 5/11, B2→Task 7, B3→Task 6, B4→Task 8, B5→Task 9, B6→Task 10, B7(опц. оракул)→вне плана (явно отложено в спеке).

**Placeholder scan:** код приведён в каждом шаге; единственные «по результату» места — Task 5 Step 3 (форма копирования harness — сверяется с `our:inventory`) и Task 11 (триаж по фактическому отчёту, по своей природе зависящий от данных). Оба — не плейсхолдеры реализации, а явные точки сверки/триажа.

**Type consistency:** `FeatureVector`/`ActiveView` (T1) → потребляются `featureKey`, `classifyCorpus` (T2), `expectedTabs`/`checkInvariants` (T3). `UiSnapshot`/`Violation` (T3) → `snapshot.ts`/`verify.ts` (T5), `formatCoverageReport`/`QueryResult` (T4). `distinctFieldRefs` (T7) — единое имя в `fieldSource.ts` и `OrderTab.tsx`. Вкладка-строка `'Индексы'` согласована между `TABS` (T6), `expectedTabs` (T3) и `App.tsx` (T6).
