# Фаза 8.4. Локальная семантическая валидация открытия — план

← [Спек](../specs/2026-06-27-phase8.4-local-semantic-validation-design.md) · [Фаза 8](../../PHASE_8.md)

TDD, по шагам. Гейт после каждого шага: `npm run test:unit` зелёный, `oracleGolden`/
`corpusRegression` байт-в-байт, `tsc` чист.

## Шаг 1. Резолвер из in-memory модели
- [ ] `src/core/metadata/buildModelResolver.ts`: `buildResolverFromTables(tables: MetaTable[]):
      MetadataResolver` — три карты (реальные `byFull`, виртуальные `byFullVirtual`,
      канонические `canonByUpper` с ТЧ), зеркало `buildYamlResolver`.
- [ ] Рефактор `buildYamlResolver` → делегат (`loadMetadataFromYaml(cfDir)` →
      `buildResolverFromTables(model.tables)`); поведение сохранить.
- [ ] `test/unit/buildModelResolver.test.ts`: реальная/виртуальная/каноническая (регистр)/
      неизвестная.

## Шаг 2. Семантический валидатор (ядро)
- [ ] `src/core/query/semanticValidator.ts`: `SemanticError`, `validateBatchSemantics`,
      `TYPE_PREFIXES`. Обход `BatchDocument` → `QueryDocument` → `QueryModel`, рекурсия в
      подзапросы-источники и подзапросы условий. Проверка существования таблиц (§2.1.1) +
      повтор явного псевдонима (§2.1.2). Позиция через `tokenize(text)` (поиск
      последовательности сегментов имени, регистронезависимо).
- [ ] `test/unit/semanticValidator.test.ts` — кейсы из спека §4 (TDD: сперва тесты).

## Шаг 3. Привратник открытия
- [ ] `src/core/query/validateBatch.ts`: `tryOpenBatch(text, resolver?, opts?)`;
      `validateBatchText(text, resolver?)` (необязательный резолвер). Без резолвера —
      прежнее поведение.
- [ ] `test/unit/validateBatch.test.ts` — дополнить (`tryOpenBatch` с/без резолвера;
      синтаксис прежде семантики).

## Шаг 4. Проводка в webview
- [ ] `src/webview/App.tsx`: `metaTablesRef` ← `metadataTree`; резолвер из неё (только при
      непустых таблицах); `loadModel` → `tryOpenBatch`; «ОК» → `validateBatchText(.., resolver)`;
      обобщить заголовок оверлея `load-error` («Не удалось открыть запрос»).
- [ ] Если e2e/unit ассертит старый заголовок «синтаксическая ошибка» — поправить.

## Шаг 5. Верификация и документация
- [ ] `npm run test:unit` (все зелёные, новые тесты учтены); `tsc`; `npm run build`.
- [ ] Обновить `docs/PHASE_8.md` (Подфаза 4 ✅; пометить, что 8.2-research реализован) и
      `docs/ROADMAP.md` (Фаза 8 → ✅).
- [ ] Коммит; merge в `master` локально.
