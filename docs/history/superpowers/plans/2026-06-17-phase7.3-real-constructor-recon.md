# Подфаза 7.3 — Разведка реального конструктора (Playwright) — План реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Собрать tooling-драйвер Playwright, который логинится в живую веб-публикацию 1С, открывает «Конструктор запроса» на реальном запросе и скриншотит его UI; доказать выполнимость (smoke) и собрать опись UI по 10 запросам.

**Architecture:** Pure-TS модуль `tooling/real-constructor/` + bundled-CLI входы (`esbuild → node`). Чистая логика (конфиг, страж рекурсии, имена скриншотов) пишется по TDD под vitest; браузерные примитивы (логин/навигация/обход вкладок) реализуются probe-driven — запустил, посмотрел скриншот, уточнил локаторы. Браузер — `chromium.launch()` из уже установленного `@playwright/test`.

**Tech Stack:** TypeScript, esbuild 0.21.5 (`--packages=external`), Playwright (`@playwright/test`, Chromium), vitest.

## Global Constraints

- Доступ к базе ТОЛЬКО через `host.docker.internal` (НЕ `localhost`). Дефолт `WEB_1C_URL = http://host.docker.internal/smallb/ru_RU/`.
- Навигация к обработке — фрагмент `#e1cib/app/Обработка.КонсольЗапросов` к базовому URL. Меню не кликаем.
- Креды из `.env` (gitignored): `USER_1C=Администратор`, `PASSWORD_1C=` (пустой). Приоритет: дефолты < `.env` < `process.env`.
- `.env`, `out/`, `tmp/` — в `.gitignore`; их НЕ коммитим. Скриншоты-референс в `docs/phase7.3-real-constructor/` — коммитим.
- Unit-тесты только на pure-логику (`test/unit/**/*.test.ts`, vitest); браузерный драйвер юнитами не покрываем — его проверка это smoke.
- Принцип устойчивости драйвера: не падать на отдельном непонятном элементе — скриншотить, писать в `tmp/real-constructor.log`, идти дальше.
- esbuild для CLI: `--platform=node --format=cjs --packages=external` (Playwright не бандлить).
- Эталонный запрос smoke: `tmp/query1c/CommonModules-УправлениеНебольшойФирмойЭлектронныеДокументыСервер-Ext-Module.bsl_9.txt`.

---

### Task 1: Загрузчик конфигурации (TDD)

**Files:**
- Create: `tooling/real-constructor/config.ts`
- Modify: `src/cli/corpusConfig.ts` (экспортировать `parseEnv` для переиспользования — DRY)
- Test: `test/unit/realConstructorConfig.test.ts`

**Interfaces:**
- Produces: `getRealConstructorConfig(root?: string): RealConstructorConfig` где `RealConstructorConfig = { webUrl: string; user: string; password: string }`. `webUrl` всегда с завершающим `/`.
- Produces (из corpusConfig): `export function parseEnv(content: string): Record<string, string>`.

- [ ] **Step 1: Экспортировать `parseEnv` из corpusConfig**

В `src/cli/corpusConfig.ts` поменять сигнатуру существующей функции с `function parseEnv(` на `export function parseEnv(` (строка 21). Больше ничего не трогать.

- [ ] **Step 2: Написать падающий тест конфига**

Create `test/unit/realConstructorConfig.test.ts`:

```ts
import { describe, it, expect, afterEach } from 'vitest';
import * as fs from 'fs';
import * as os from 'os';
import * as path from 'path';
import { getRealConstructorConfig } from '../../tooling/real-constructor/config';

function tmpRoot(envContent: string): string {
  const dir = fs.mkdtempSync(path.join(os.tmpdir(), 'rc-cfg-'));
  fs.writeFileSync(path.join(dir, '.env'), envContent, 'utf8');
  return dir;
}

const saved = { ...process.env };
afterEach(() => {
  process.env = { ...saved };
});

describe('getRealConstructorConfig', () => {
  it('берёт значения из .env', () => {
    const root = tmpRoot('WEB_1C_URL=http://host.docker.internal/smallb/ru_RU/\nUSER_1C=Администратор\nPASSWORD_1C=\n');
    const cfg = getRealConstructorConfig(root);
    expect(cfg.webUrl).toBe('http://host.docker.internal/smallb/ru_RU/');
    expect(cfg.user).toBe('Администратор');
    expect(cfg.password).toBe('');
  });

  it('добавляет завершающий слэш к webUrl', () => {
    const root = tmpRoot('WEB_1C_URL=http://host.docker.internal/smallb/ru_RU\n');
    expect(getRealConstructorConfig(root).webUrl).toBe('http://host.docker.internal/smallb/ru_RU/');
  });

  it('дефолт webUrl, если .env отсутствует', () => {
    const dir = fs.mkdtempSync(path.join(os.tmpdir(), 'rc-cfg-empty-'));
    expect(getRealConstructorConfig(dir).webUrl).toBe('http://host.docker.internal/smallb/ru_RU/');
  });

  it('process.env перекрывает .env', () => {
    const root = tmpRoot('USER_1C=ИзФайла\n');
    process.env.USER_1C = 'ИзОкружения';
    expect(getRealConstructorConfig(root).user).toBe('ИзОкружения');
  });
});
```

- [ ] **Step 3: Запустить тест — убедиться, что падает**

Run: `npx vitest run test/unit/realConstructorConfig.test.ts`
Expected: FAIL — `Cannot find module '../../tooling/real-constructor/config'`.

- [ ] **Step 4: Реализовать config.ts**

Create `tooling/real-constructor/config.ts`:

```ts
/**
 * Конфигурация драйвера реального конструктора. Источники (по возрастанию приоритета):
 * дефолты < корневой `.env` < `process.env`. Парсер `.env` переиспользуется из corpusConfig (DRY).
 */
import * as fs from 'fs';
import * as path from 'path';
import { parseEnv } from '../../src/cli/corpusConfig';

export interface RealConstructorConfig {
  /** Базовый URL веб-публикации, всегда с завершающим `/`. */
  webUrl: string;
  user: string;
  password: string;
}

const DEFAULTS: Record<string, string> = {
  WEB_1C_URL: 'http://host.docker.internal/smallb/ru_RU/',
  USER_1C: '',
  PASSWORD_1C: '',
};

export function getRealConstructorConfig(root = process.cwd()): RealConstructorConfig {
  const envFile = path.join(root, '.env');
  const fileEnv = fs.existsSync(envFile) ? parseEnv(fs.readFileSync(envFile, 'utf8')) : {};
  const resolve = (key: string): string => process.env[key] ?? fileEnv[key] ?? DEFAULTS[key] ?? '';
  let webUrl = resolve('WEB_1C_URL').trim();
  if (!webUrl.endsWith('/')) webUrl += '/';
  return { webUrl, user: resolve('USER_1C'), password: resolve('PASSWORD_1C') };
}
```

- [ ] **Step 5: Запустить тест — убедиться, что проходит**

Run: `npx vitest run test/unit/realConstructorConfig.test.ts`
Expected: PASS (4 теста).

- [ ] **Step 6: Коммит**

```bash
git add tooling/real-constructor/config.ts src/cli/corpusConfig.ts test/unit/realConstructorConfig.test.ts
git commit -m "feat(phase7.3): загрузчик конфигурации драйвера реального конструктора

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: Страж рекурсии обхода под-окон (TDD)

**Files:**
- Create: `tooling/real-constructor/recursionGuard.ts`
- Test: `test/unit/realConstructorRecursionGuard.test.ts`

**Interfaces:**
- Produces: `class RecursionGuard` с конструктором `new RecursionGuard({ maxDepth?: number })` (дефолт `maxDepth=3`), методом `shouldEnter(title: string, depth: number): boolean` и геттером `visitedCount: number`.

- [ ] **Step 1: Написать падающий тест**

Create `test/unit/realConstructorRecursionGuard.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { RecursionGuard } from '../../tooling/real-constructor/recursionGuard';

describe('RecursionGuard', () => {
  it('пускает на новый заголовок в пределах глубины', () => {
    const g = new RecursionGuard();
    expect(g.shouldEnter('Выбор поля', 1)).toBe(true);
  });

  it('не пускает повторно на тот же заголовок (без учёта регистра/пробелов)', () => {
    const g = new RecursionGuard();
    expect(g.shouldEnter('Выбор поля', 1)).toBe(true);
    expect(g.shouldEnter('  выбор ПОЛЯ  ', 2)).toBe(false);
  });

  it('не пускает глубже maxDepth', () => {
    const g = new RecursionGuard({ maxDepth: 2 });
    expect(g.shouldEnter('A', 2)).toBe(true);
    expect(g.shouldEnter('B', 3)).toBe(false);
  });

  it('не пускает на пустой заголовок', () => {
    const g = new RecursionGuard();
    expect(g.shouldEnter('   ', 1)).toBe(false);
  });

  it('считает посещённые окна', () => {
    const g = new RecursionGuard();
    g.shouldEnter('A', 1);
    g.shouldEnter('B', 1);
    g.shouldEnter('A', 1);
    expect(g.visitedCount).toBe(2);
  });
});
```

- [ ] **Step 2: Запустить — убедиться, что падает**

Run: `npx vitest run test/unit/realConstructorRecursionGuard.test.ts`
Expected: FAIL — модуль не найден.

- [ ] **Step 3: Реализовать recursionGuard.ts**

Create `tooling/real-constructor/recursionGuard.ts`:

```ts
/**
 * Страж рекурсивного обхода под-окон конструктора: ограничивает глубину и не даёт
 * повторно входить в уже виденное окно (ключ — нормализованный заголовок).
 */
export interface RecursionGuardOptions {
  maxDepth?: number;
}

export class RecursionGuard {
  private readonly maxDepth: number;
  private readonly seen = new Set<string>();

  constructor(opts: RecursionGuardOptions = {}) {
    this.maxDepth = opts.maxDepth ?? 3;
  }

  /** Можно ли войти в окно `title` на глубине `depth`? Помечает заголовок виденным при входе. */
  shouldEnter(title: string, depth: number): boolean {
    if (depth > this.maxDepth) return false;
    const key = title.trim().toLowerCase();
    if (key === '') return false;
    if (this.seen.has(key)) return false;
    this.seen.add(key);
    return true;
  }

  get visitedCount(): number {
    return this.seen.size;
  }
}
```

- [ ] **Step 4: Запустить — убедиться, что проходит**

Run: `npx vitest run test/unit/realConstructorRecursionGuard.test.ts`
Expected: PASS (5 тестов).

- [ ] **Step 5: Коммит**

```bash
git add tooling/real-constructor/recursionGuard.ts test/unit/realConstructorRecursionGuard.test.ts
git commit -m "feat(phase7.3): страж рекурсии обхода под-окон конструктора

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: Имена и сохранение скриншотов (TDD на именах)

**Files:**
- Create: `tooling/real-constructor/screenshot.ts`
- Test: `test/unit/realConstructorScreenshot.test.ts`

**Interfaces:**
- Produces: `screenshotName(index: number, label: string): string` — `<NN>-<slug>.png`, NN с ведущим нулём, кириллица сохраняется.
- Produces: `async saveScreenshot(page: Page, dir: string, index: number, label: string): Promise<string>` — создаёт `dir`, сохраняет PNG, возвращает путь. (Не покрывается юнитом — браузер.)

- [ ] **Step 1: Написать падающий тест имени**

Create `test/unit/realConstructorScreenshot.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { screenshotName } from '../../tooling/real-constructor/screenshot';

describe('screenshotName', () => {
  it('дополняет индекс ведущим нулём', () => {
    expect(screenshotName(1, 'Таблицы и поля')).toBe('01-таблицы-и-поля.png');
  });

  it('двузначный индекс не трогает', () => {
    expect(screenshotName(12, 'Итоги')).toBe('12-итоги.png');
  });

  it('схлопывает спецсимволы в дефисы и обрезает края', () => {
    expect(screenshotName(3, '  Объединения / Псевдонимы!  ')).toBe('03-объединения-псевдонимы.png');
  });

  it('пустая подпись → screen', () => {
    expect(screenshotName(0, '   ')).toBe('00-screen.png');
  });
});
```

- [ ] **Step 2: Запустить — убедиться, что падает**

Run: `npx vitest run test/unit/realConstructorScreenshot.test.ts`
Expected: FAIL — модуль не найден.

- [ ] **Step 3: Реализовать screenshot.ts**

Create `tooling/real-constructor/screenshot.ts`:

```ts
import * as fs from 'fs';
import * as path from 'path';
import type { Page } from '@playwright/test';

/** Безопасное имя файла из произвольной подписи вкладки/окна; кириллица сохраняется. */
export function screenshotName(index: number, label: string): string {
  const nn = String(index).padStart(2, '0');
  const slug =
    label
      .trim()
      .toLowerCase()
      .replace(/[^a-zа-яё0-9]+/gi, '-')
      .replace(/^-+|-+$/g, '') || 'screen';
  return `${nn}-${slug}.png`;
}

/** Сохранить скриншот страницы в каталог под предсказуемым именем. Возвращает путь. */
export async function saveScreenshot(
  page: Page,
  dir: string,
  index: number,
  label: string,
): Promise<string> {
  fs.mkdirSync(dir, { recursive: true });
  const file = path.join(dir, screenshotName(index, label));
  await page.screenshot({ path: file, fullPage: false });
  return file;
}
```

- [ ] **Step 4: Запустить — убедиться, что проходит**

Run: `npx vitest run test/unit/realConstructorScreenshot.test.ts`
Expected: PASS (4 теста).

- [ ] **Step 5: Коммит**

```bash
git add tooling/real-constructor/screenshot.ts test/unit/realConstructorScreenshot.test.ts
git commit -m "feat(phase7.3): имена и сохранение скриншотов

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: Установка Chromium + npm-скрипты + лог (инфраструктура)

**Files:**
- Modify: `package.json` (scripts)
- Create: `tooling/real-constructor/logger.ts`
- Test: `test/unit/realConstructorLogger.test.ts`

**Interfaces:**
- Produces: `createLogger(file: string): { info(msg: string): void; warn(msg: string): void; lines(): string[] }` — пишет строки `[LEVEL] msg` в файл (append) и хранит их в памяти для проверки.

- [ ] **Step 1: Падающий тест логгера**

Create `test/unit/realConstructorLogger.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import * as fs from 'fs';
import * as os from 'os';
import * as path from 'path';
import { createLogger } from '../../tooling/real-constructor/logger';

describe('createLogger', () => {
  it('пишет строки в память и в файл', () => {
    const file = path.join(fs.mkdtempSync(path.join(os.tmpdir(), 'rc-log-')), 'r.log');
    const log = createLogger(file);
    log.info('старт');
    log.warn('окно не найдено');
    expect(log.lines()).toEqual(['[INFO] старт', '[WARN] окно не найдено']);
    expect(fs.readFileSync(file, 'utf8')).toBe('[INFO] старт\n[WARN] окно не найдено\n');
  });
});
```

- [ ] **Step 2: Запустить — убедиться, что падает**

Run: `npx vitest run test/unit/realConstructorLogger.test.ts`
Expected: FAIL — модуль не найден.

- [ ] **Step 3: Реализовать logger.ts**

Create `tooling/real-constructor/logger.ts`:

```ts
import * as fs from 'fs';
import * as path from 'path';

export interface Logger {
  info(msg: string): void;
  warn(msg: string): void;
  lines(): string[];
}

/** Простой логгер: дублирует строки в память и дописывает в файл (с созданием каталога). */
export function createLogger(file: string): Logger {
  fs.mkdirSync(path.dirname(file), { recursive: true });
  fs.writeFileSync(file, '', 'utf8');
  const buf: string[] = [];
  const write = (level: string, msg: string): void => {
    const line = `[${level}] ${msg}`;
    buf.push(line);
    fs.appendFileSync(file, line + '\n', 'utf8');
  };
  return {
    info: (m) => write('INFO', m),
    warn: (m) => write('WARN', m),
    lines: () => [...buf],
  };
}
```

- [ ] **Step 4: Запустить — убедиться, что проходит**

Run: `npx vitest run test/unit/realConstructorLogger.test.ts`
Expected: PASS.

- [ ] **Step 5: Добавить npm-скрипты**

В `package.json` в раздел `scripts` добавить (рядом с `test:e2e`):

```json
"real:setup": "npx playwright install chromium && npx playwright install-deps chromium",
"real:smoke": "esbuild tooling/real-constructor/smoke.ts --bundle --outfile=out/real-constructor/smoke.js --platform=node --format=cjs --packages=external && node out/real-constructor/smoke.js",
"real:inventory": "esbuild tooling/real-constructor/inventory.ts --bundle --outfile=out/real-constructor/inventory.js --platform=node --format=cjs --packages=external && node out/real-constructor/inventory.js"
```

- [ ] **Step 6: Установить Chromium**

Run: `npm run real:setup`
Expected: скачивание Chromium завершается успехом; `ls ~/.cache/ms-playwright` показывает каталог `chromium-*`.

- [ ] **Step 7: Коммит**

```bash
git add tooling/real-constructor/logger.ts test/unit/realConstructorLogger.test.ts package.json
git commit -m "chore(phase7.3): логгер обхода + npm-скрипты real:setup/smoke/inventory

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: Сессия — запуск браузера и логин (probe-driven)

> Браузерный код: «тест» — запуск probe-скрипта и осмотр скриншота. Локаторы формы логина заранее неизвестны — уточняются по скриншоту.

**Files:**
- Create: `tooling/real-constructor/session.ts`
- Create (временный probe): `tooling/real-constructor/probe.ts`

**Interfaces:**
- Produces: `async launch(): Promise<Session>` где `Session = { browser: Browser; context: BrowserContext; page: Page; cfg: RealConstructorConfig }`.
- Produces: `async login(s: Session): Promise<void>` — после возврата страница на рабочем столе 1С.
- Produces: `async close(s: Session): Promise<void>`.

- [ ] **Step 1: Реализовать session.ts (скелет логина)**

Create `tooling/real-constructor/session.ts`:

```ts
import { chromium, type Browser, type BrowserContext, type Page } from '@playwright/test';
import { getRealConstructorConfig, type RealConstructorConfig } from './config';

export interface Session {
  browser: Browser;
  context: BrowserContext;
  page: Page;
  cfg: RealConstructorConfig;
}

export async function launch(): Promise<Session> {
  const cfg = getRealConstructorConfig();
  const browser = await chromium.launch({ headless: true });
  const context = await browser.newContext({ viewport: { width: 1600, height: 1000 } });
  const page = await context.newPage();
  return { browser, context, page, cfg };
}

export async function login(s: Session): Promise<void> {
  const { page, cfg } = s;
  await page.goto(cfg.webUrl, { waitUntil: 'networkidle' });
  // ВЕБ-КЛИЕНТ 1С: форма логина рендерится JS. Стартовая стратегия — поля по порядку.
  // УТОЧНИТЬ по скриншоту 00-landing.png (Step 3): если есть поля логина/пароля —
  // заполнить их, нажать кнопку входа; если автологин — этот блок может быть no-op.
  const inputs = page.locator('input:visible');
  if ((await inputs.count()) > 0 && cfg.user) {
    await inputs.nth(0).fill(cfg.user);
    if ((await inputs.count()) > 1) await inputs.nth(1).fill(cfg.password);
    await page.keyboard.press('Enter');
  }
  await page.waitForLoadState('networkidle');
}

export async function close(s: Session): Promise<void> {
  await s.context.close();
  await s.browser.close();
}
```

- [ ] **Step 2: Написать probe-скрипт (логин + два скриншота)**

Create `tooling/real-constructor/probe.ts`:

```ts
import * as path from 'path';
import { launch, login, close } from './session';
import { saveScreenshot } from './screenshot';

async function main(): Promise<void> {
  const dir = path.resolve('tmp/real-constructor-probe');
  const s = await launch();
  try {
    await s.page.goto(s.cfg.webUrl, { waitUntil: 'networkidle' });
    await saveScreenshot(s.page, dir, 0, 'landing');
    await login(s);
    await saveScreenshot(s.page, dir, 1, 'after-login');
    console.log('probe ok, page title:', await s.page.title());
  } finally {
    await close(s);
  }
}

main().catch((e) => {
  console.error(e);
  process.exit(1);
});
```

- [ ] **Step 3: Запустить probe и осмотреть скриншоты**

Run: `esbuild tooling/real-constructor/probe.ts --bundle --outfile=out/real-constructor/probe.js --platform=node --format=cjs --packages=external && node out/real-constructor/probe.js`
Expected: создаются `tmp/real-constructor-probe/00-landing.png` и `01-after-login.png`.
**Осмотреть оба скриншота** (Read tool на PNG). На `00-landing` — увидеть форму логина и реальные поля; на `01-after-login` — рабочий стол 1С (панель разделов / начальная страница).

- [ ] **Step 4: Уточнить локаторы логина**

Если `01-after-login.png` НЕ показывает рабочий стол (логин не прошёл) — поправить `login()` в `session.ts` под реальные поля, увиденные на `00-landing.png` (например, локаторы по `placeholder`/подписи, отдельная кнопка «Войти» вместо Enter). Повторять Step 3 ↔ Step 4, пока `01-after-login.png` не покажет рабочий стол.

- [ ] **Step 5: Коммит**

```bash
git add tooling/real-constructor/session.ts tooling/real-constructor/probe.ts
git commit -m "feat(phase7.3): сессия браузера + логин в веб-клиент 1С (probe-verified)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 6: Открытие «Консоли запросов» (probe-driven)

**Files:**
- Create: `tooling/real-constructor/console.ts`
- Modify: `tooling/real-constructor/probe.ts` (добавить шаг навигации)

**Interfaces:**
- Consumes: `Session` из `session.ts`.
- Produces: `async openQueryConsole(s: Session): Promise<void>` — после возврата открыта форма обработки «Консоль запросов».

- [ ] **Step 1: Реализовать console.ts**

Create `tooling/real-constructor/console.ts`:

```ts
import type { Session } from './session';

const NAV = '#e1cib/app/Обработка.КонсольЗапросов';

/** Открыть обработку «Консоль запросов» прямой навигационной ссылкой 1С. */
export async function openQueryConsole(s: Session): Promise<void> {
  const { page, cfg } = s;
  await page.goto(cfg.webUrl + NAV, { waitUntil: 'networkidle' });
  // Форма обработки грузится асинхронно — ждём появления управляемой формы.
  await page.waitForLoadState('networkidle');
}
```

- [ ] **Step 2: Добавить шаг навигации в probe.ts**

В `tooling/real-constructor/probe.ts` после `await saveScreenshot(s.page, dir, 1, 'after-login');` добавить:

```ts
    const { openQueryConsole } = await import('./console');
    await openQueryConsole(s);
    await saveScreenshot(s.page, dir, 2, 'query-console');
```

- [ ] **Step 3: Запустить probe и осмотреть**

Run: `esbuild tooling/real-constructor/probe.ts --bundle --outfile=out/real-constructor/probe.js --platform=node --format=cjs --packages=external && node out/real-constructor/probe.js`
Expected: `tmp/real-constructor-probe/02-query-console.png` показывает форму обработки «Консоль запросов» (поле для текста запроса, кнопки).

- [ ] **Step 4: Уточнить ожидание формы**

Если `02-query-console.png` пустой/не догрузился — добавить в `openQueryConsole` явное ожидание видимого элемента формы (например, `await page.getByRole('textbox').first().waitFor()` или ожидание заголовка окна «Консоль запросов»), опираясь на то, что видно на скриншоте. Повторять Step 3 ↔ Step 4 до стабильного скриншота формы.

- [ ] **Step 5: Коммит**

```bash
git add tooling/real-constructor/console.ts tooling/real-constructor/probe.ts
git commit -m "feat(phase7.3): открытие Консоли запросов навигационной ссылкой (probe-verified)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 7: Загрузка запроса в конструктор + обход вкладок (probe-driven)

**Files:**
- Create: `tooling/real-constructor/constructor.ts`
- Modify: `tooling/real-constructor/probe.ts`

**Interfaces:**
- Consumes: `Session`, `RecursionGuard`, `saveScreenshot`, `Logger`.
- Produces: `async loadQueryIntoConstructor(s: Session, queryText: string): Promise<void>` — текст вставлен в поле, открыт модальный диалог «Конструктор запроса».
- Produces: `async walkTabs(s: Session, shotDir: string, log: Logger, guard: RecursionGuard): Promise<number>` — обходит вкладки конструктора, скриншотит каждую и (рекурсивно, через `guard`) под-окна; возвращает число сохранённых скриншотов.

- [ ] **Step 1: Реализовать constructor.ts**

Create `tooling/real-constructor/constructor.ts`:

```ts
import type { Session } from './session';
import type { Logger } from './logger';
import { RecursionGuard } from './recursionGuard';
import { saveScreenshot } from './screenshot';

/**
 * Вставить текст запроса в поле «Консоли запросов» и открыть «Конструктор запроса».
 * Локаторы поля и кнопки/команды уточняются по скриншоту во время probe.
 */
export async function loadQueryIntoConstructor(s: Session, queryText: string): Promise<void> {
  const { page } = s;
  const field = page.getByRole('textbox').first();
  await field.click();
  await field.fill(queryText);
  // Команда «Конструктор запроса»: стартово — пункт по видимому тексту. УТОЧНИТЬ по скриншоту.
  await page.getByText('Конструктор запроса', { exact: false }).first().click();
  await page.waitForLoadState('networkidle');
}

/**
 * Обойти вкладки конструктора: клик по вкладке → скриншот → рекурсивный заход в под-окна
 * (через guard). Возвращает число сохранённых скриншотов. Не падает на отдельном элементе —
 * пишет WARN в лог и продолжает.
 */
export async function walkTabs(
  s: Session,
  shotDir: string,
  log: Logger,
  guard: RecursionGuard,
): Promise<number> {
  const { page } = s;
  // Стартовый список вкладок классического конструктора. Реальный набор уточняется по скриншотам.
  const TABS = [
    'Таблицы и поля',
    'Связи',
    'Группировка',
    'Условия',
    'Дополнительно',
    'Объединения/Псевдонимы',
    'Порядок',
    'Итоги',
  ];
  let count = 0;
  for (let i = 0; i < TABS.length; i++) {
    const tab = TABS[i];
    try {
      const loc = page.getByText(tab, { exact: false }).first();
      if ((await loc.count()) === 0) {
        log.warn(`вкладка не найдена: ${tab}`);
        continue;
      }
      await loc.click();
      await page.waitForTimeout(300);
      await saveScreenshot(page, shotDir, i + 1, tab);
      count++;
      if (guard.shouldEnter(tab, 1)) {
        log.info(`вкладка снята: ${tab}`);
      }
    } catch (e) {
      log.warn(`ошибка на вкладке ${tab}: ${(e as Error).message}`);
    }
  }
  return count;
}
```

- [ ] **Step 2: Добавить загрузку запроса в probe.ts**

В `tooling/real-constructor/probe.ts` после скриншота `02-query-console` добавить:

```ts
    const fs = await import('fs');
    const { loadQueryIntoConstructor, walkTabs } = await import('./constructor');
    const { createLogger } = await import('./logger');
    const { RecursionGuard } = await import('./recursionGuard');
    const queryText = fs.readFileSync(
      'tmp/query1c/CommonModules-УправлениеНебольшойФирмойЭлектронныеДокументыСервер-Ext-Module.bsl_9.txt',
      'utf8',
    );
    await loadQueryIntoConstructor(s, queryText);
    await saveScreenshot(s.page, dir, 3, 'constructor-opened');
    const log = createLogger('tmp/real-constructor.log');
    const n = await walkTabs(s, dir, log, new RecursionGuard());
    console.log('вкладок снято:', n);
```

- [ ] **Step 3: Запустить probe и осмотреть**

Run: `esbuild tooling/real-constructor/probe.ts --bundle --outfile=out/real-constructor/probe.js --platform=node --format=cjs --packages=external && node out/real-constructor/probe.js`
Expected: `03-constructor-opened.png` показывает модальный диалог конструктора; появляются `01-…`/`02-…` со скриншотами вкладок.

- [ ] **Step 4: Уточнить локаторы поля/команды/вкладок**

По скриншотам `02`/`03` уточнить в `constructor.ts`: реальный локатор поля запроса, как именно вызывается «Конструктор запроса» (кнопка командной панели / контекстное меню), точные подписи вкладок. Повторять Step 3 ↔ Step 4, пока `03-constructor-opened.png` не покажет открытый конструктор и не появятся скриншоты вкладок.

- [ ] **Step 5: Коммит**

```bash
git add tooling/real-constructor/constructor.ts tooling/real-constructor/probe.ts
git commit -m "feat(phase7.3): загрузка запроса в конструктор + обход вкладок (probe-verified)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 8: Рекурсивный обход под-окон вкладок

**Files:**
- Modify: `tooling/real-constructor/constructor.ts`

**Interfaces:**
- Produces: `async walkSubWindows(s: Session, shotDir: string, log: Logger, guard: RecursionGuard, depth: number, baseIndex: number): Promise<number>` — на текущей вкладке находит кнопки, открывающие диалоги, заходит в каждую (через `guard.shouldEnter(title, depth)`), скриншотит, рекурсирует, закрывает. Вызывается из `walkTabs`.

- [ ] **Step 1: Добавить walkSubWindows и подключить в walkTabs**

В `tooling/real-constructor/constructor.ts` добавить функцию и вызвать её внутри цикла `walkTabs` вместо текущего блока `if (guard.shouldEnter(...))`:

```ts
/**
 * На текущей вкладке/окне найти кнопки, открывающие под-диалоги, и рекурсивно их обойти.
 * Защита от зацикливания — через guard (глубина + дедуп заголовков). Возвращает число скриншотов.
 */
export async function walkSubWindows(
  s: Session,
  shotDir: string,
  log: Logger,
  guard: RecursionGuard,
  depth: number,
  baseIndex: number,
): Promise<number> {
  const { page } = s;
  let count = 0;
  const buttons = page.getByRole('button');
  const total = await buttons.count();
  for (let i = 0; i < total; i++) {
    const btn = buttons.nth(i);
    const title = ((await btn.getAttribute('title')) ?? (await btn.innerText()).trim()) || '';
    if (!guard.shouldEnter(`d${depth}:${title}:${i}`, depth)) continue;
    try {
      await btn.click({ timeout: 1500 });
      await page.waitForTimeout(250);
      // Под-диалог считается открытым, если появился новый видимый dialog.
      const dialog = page.getByRole('dialog').last();
      if ((await dialog.count()) > 0) {
        await saveScreenshot(page, shotDir, baseIndex * 100 + i, `d${depth}-${title || 'окно'}`);
        count++;
        count += await walkSubWindows(s, shotDir, log, guard, depth + 1, baseIndex * 100 + i);
        await page.keyboard.press('Escape');
        await page.waitForTimeout(150);
      }
    } catch (e) {
      log.warn(`под-окно d${depth} #${i} (${title}): ${(e as Error).message}`);
    }
  }
  return count;
}
```

И в `walkTabs` заменить блок:

```ts
      if (guard.shouldEnter(tab, 1)) {
        log.info(`вкладка снята: ${tab}`);
      }
```

на:

```ts
      log.info(`вкладка снята: ${tab}`);
      count += await walkSubWindows(s, shotDir, log, guard, 2, i + 1);
```

- [ ] **Step 2: Запустить probe и осмотреть**

Run: `esbuild tooling/real-constructor/probe.ts --bundle --outfile=out/real-constructor/probe.js --platform=node --format=cjs --packages=external && node out/real-constructor/probe.js`
Expected: появляются скриншоты под-окон (имена с префиксом `d2-…`); `tmp/real-constructor.log` без неожиданных падений; probe завершается без исключения.

- [ ] **Step 3: Откалибровать (если нужно)**

Если probe зацикливается или открывает мусорные окна — уменьшить `maxDepth` (создавать `new RecursionGuard({ maxDepth: 2 })`) или сузить набор кнопок. Цель — обход завершается, под-окна сняты.

- [ ] **Step 4: Коммит**

```bash
git add tooling/real-constructor/constructor.ts
git commit -m "feat(phase7.3): рекурсивный обход под-окон вкладок конструктора

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 9: Вход `smoke` + приёмка выполнимости

**Files:**
- Create: `tooling/real-constructor/smoke.ts`
- Delete: `tooling/real-constructor/probe.ts` (его роль выполнена — заменяется smoke)

**Interfaces:**
- Consumes: `launch`/`login`/`close`, `openQueryConsole`, `loadQueryIntoConstructor`/`walkTabs`, `createLogger`, `RecursionGuard`.

- [ ] **Step 1: Реализовать smoke.ts**

Create `tooling/real-constructor/smoke.ts`:

```ts
import * as fs from 'fs';
import * as path from 'path';
import { launch, login, close } from './session';
import { openQueryConsole } from './console';
import { loadQueryIntoConstructor, walkTabs } from './constructor';
import { saveScreenshot } from './screenshot';
import { createLogger } from './logger';
import { RecursionGuard } from './recursionGuard';

const SAMPLE = 'tmp/query1c/CommonModules-УправлениеНебольшойФирмойЭлектронныеДокументыСервер-Ext-Module.bsl_9.txt';

async function main(): Promise<void> {
  const slug = path.basename(SAMPLE).replace(/\.[^.]+$/, '');
  const dir = path.resolve('docs/phase7.3-real-constructor', slug);
  const log = createLogger('tmp/real-constructor.log');
  const s = await launch();
  try {
    await login(s);
    await openQueryConsole(s);
    const text = fs.readFileSync(SAMPLE, 'utf8');
    await loadQueryIntoConstructor(s, text);
    await saveScreenshot(s.page, dir, 0, 'constructor-opened');
    const n = await walkTabs(s, dir, log, new RecursionGuard());
    log.info(`smoke готов: вкладок ${n}, скриншоты в ${dir}`);
    console.log(`smoke OK: ${n} вкладок, скриншоты → ${dir}`);
  } finally {
    await close(s);
  }
}

main().catch((e) => {
  console.error(e);
  process.exit(1);
});
```

- [ ] **Step 2: Удалить probe.ts**

```bash
git rm tooling/real-constructor/probe.ts
```

- [ ] **Step 3: Запустить smoke (приёмка выполнимости)**

Run: `npm run real:smoke`
Expected: вывод `smoke OK: N вкладок`; в `docs/phase7.3-real-constructor/CommonModules-…_9/` лежат `00-constructor-opened.png` + скриншоты вкладок. **Осмотреть `00-constructor-opened.png`** — должен показывать реальный конструктор с загруженным запросом `ЗаказПокупателя` (заполнены Таблицы/Поля). Это и есть доказательство выполнимости.

- [ ] **Step 4: Коммит (скрипт + скриншоты-референс)**

```bash
git add tooling/real-constructor/smoke.ts
git add docs/phase7.3-real-constructor/
git commit -m "feat(phase7.3): вход smoke — приёмка выполнимости на эталонном запросе

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 10: Вход `inventory` — 10 запросов + опись UI

**Files:**
- Create: `tooling/real-constructor/inventory.ts`
- Create: `docs/phase7.3-real-constructor/INVENTORY.md` (человеко-проходом по скриншотам)

**Interfaces:**
- Consumes: те же примитивы, что smoke; прогон в цикле по 10 запросам.

- [ ] **Step 1: Реализовать inventory.ts**

Create `tooling/real-constructor/inventory.ts`:

```ts
import * as fs from 'fs';
import * as path from 'path';
import { launch, login, close } from './session';
import { openQueryConsole } from './console';
import { loadQueryIntoConstructor, walkTabs } from './constructor';
import { saveScreenshot } from './screenshot';
import { createLogger } from './logger';
import { RecursionGuard } from './recursionGuard';

// 10 сложных репрезентативных запросов (крупнейшие по размеру из корпуса).
const QUERIES = [
  'Documents-ОтчетКомиссионера-Ext-ManagerModule.bsl_24.txt',
  'CommonModules-ЗарплатаКадрыОбщиеНаборыДанныхБазовый-Ext-Module.bsl_1.txt',
  'CommonModules-УчетСтраховыхВзносовАрхивныеАлгоритмы-Ext-Module.bsl_249.txt',
  'Documents-ЗаказПокупателя-Ext-ManagerModule.bsl_89.txt',
  'CommonModules-УчетСтраховыхВзносов-Ext-Module.bsl_66.txt',
  'Documents-ПриходнаяНакладная-Ext-ManagerModule.bsl_44.txt',
  'Reports-КарточкаУчетаПоСтраховымВзносам-Ext-ObjectModule.bsl_3.txt',
  'CommonModules-СкидкиНаценкиСервер-Ext-Module.bsl_19.txt',
  'Documents-ОтчетОРозничныхПродажах-Ext-ManagerModule.bsl_27.txt',
  'CommonModules-УправлениеНебольшойФирмойЭлектронныеДокументыСервер-Ext-Module.bsl_9.txt',
];

async function main(): Promise<void> {
  const log = createLogger('tmp/real-constructor.log');
  const s = await launch();
  try {
    await login(s);
    for (const name of QUERIES) {
      const file = path.join('tmp/query1c', name);
      if (!fs.existsSync(file)) {
        log.warn(`нет файла запроса: ${name}`);
        continue;
      }
      const slug = name.replace(/\.[^.]+$/, '');
      const dir = path.resolve('docs/phase7.3-real-constructor', slug);
      try {
        await openQueryConsole(s);
        await loadQueryIntoConstructor(s, fs.readFileSync(file, 'utf8'));
        await saveScreenshot(s.page, dir, 0, 'constructor-opened');
        const n = await walkTabs(s, dir, log, new RecursionGuard());
        log.info(`${slug}: вкладок ${n}`);
        console.log(`${slug}: ${n} вкладок`);
      } catch (e) {
        log.warn(`${slug}: ${(e as Error).message}`);
      }
    }
  } finally {
    await close(s);
  }
}

main().catch((e) => {
  console.error(e);
  process.exit(1);
});
```

- [ ] **Step 2: Запустить inventory**

Run: `npm run real:inventory`
Expected: для каждого из 10 запросов появляется каталог в `docs/phase7.3-real-constructor/<slug>/` со скриншотами; `tmp/real-constructor.log` фиксирует, что не открылось.

- [ ] **Step 3: Написать INVENTORY.md по скриншотам**

Прочитать собранные PNG (Read tool) и составить `docs/phase7.3-real-constructor/INVENTORY.md`: по каждой вкладке перечислить все панели, списки, поля, кнопки и под-окна (рекурсивно), отметить расхождения с нашим webview как кандидатов в задачи. Структура — раздел на вкладку, под-разделы на под-окна.

- [ ] **Step 4: Коммит**

```bash
git add tooling/real-constructor/inventory.ts docs/phase7.3-real-constructor/
git commit -m "feat(phase7.3): вход inventory (10 запросов) + опись UI реального конструктора

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 11: Финал — полный прогон тестов и обновление PHASE_7.md

**Files:**
- Modify: `docs/PHASE_7.md` (добавить раздел «Подфаза 3 — ✅ закрыто»)
- Modify: `docs/ROADMAP.md` (если требуется отметка)

- [ ] **Step 1: Полный unit-сьют**

Run: `npm run test:unit`
Expected: PASS, включая 4 новых файла (`realConstructorConfig`, `realConstructorRecursionGuard`, `realConstructorScreenshot`, `realConstructorLogger`); прежнее число тестов не уменьшилось.

- [ ] **Step 2: Дописать раздел в PHASE_7.md**

В `docs/PHASE_7.md` после раздела «Подфаза 2» добавить:

```markdown
## Подфаза 3. Разведка реального конструктора через Playwright — ✅ закрыто

Tooling-драйвер `tooling/real-constructor/` (config/session/console/constructor/
screenshot/logger/recursionGuard) логинится в живую веб-публикацию
(`host.docker.internal/smallb`), открывает обработку «Консоль запросов»
(`#e1cib/app/Обработка.КонсольЗапросов`), загружает запрос в реальный «Конструктор
запроса» и скриншотит вкладки с рекурсивным обходом под-окон. `npm run real:smoke` —
приёмка выполнимости на эталонном запросе; `npm run real:inventory` — 10 сложных
запросов → скриншоты + `docs/phase7.3-real-constructor/INVENTORY.md` (опись UI).
Pure-логика покрыта vitest; драйвер — smoke-приёмкой. Спек+план:
[спек](../specs/2026-06-17-phase7.3-real-constructor-recon-design.md) ·
[план](2026-06-17-phase7.3-real-constructor-recon.md).

Граница с Подфазой 4 (корпусной прогон, оракул = реальный конструктор) — в спеке.
```

- [ ] **Step 3: Финальный коммит**

```bash
git add docs/PHASE_7.md docs/ROADMAP.md
git commit -m "docs(phase7.3): подфаза 3 закрыта — разведка реального конструктора

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Self-Review

**1. Покрытие спека:**
- Драйвер `tooling/real-constructor/` (config/session/console/constructor/screenshot) — Tasks 1,3,5,6,7,8 (+ logger/recursionGuard как выделенные модули — Tasks 2,4). ✅
- CLI `real:setup`/`real:smoke`/`real:inventory` — Tasks 4,9,10. ✅
- Навигационная ссылка `#e1cib/app/Обработка.КонсольЗапросов` — Task 6. ✅
- Конфиг из `.env` с приоритетом — Task 1. ✅
- Страж рекурсии (глубина + дедуп) — Tasks 2,8. ✅
- Устойчивость (не падать, лог в `tmp/`) — Task 4 (logger) + try/catch в Tasks 7,8,10. ✅
- Скриншоты в `docs/phase7.3-real-constructor/<slug>/`, `INVENTORY.md` — Tasks 9,10. ✅
- Юниты только на pure-логику; драйвер — smoke — Tasks 1–4 (юниты), 9 (smoke-приёмка). ✅
- Эталонный запрос smoke + 10 сложных — Tasks 9,10. ✅
- Установка Chromium — Task 4. ✅
- Граница с 7.4 — Task 11 (PHASE_7.md). ✅

**2. Плейсхолдеры:** Браузерные шаги намеренно probe-driven (локаторы уточняются по скриншотам) — это не плейсхолдер, а единственный честный способ работать с неизвестным DOM веб-клиента 1С; стартовый код приведён полностью, итерация явная (Step «осмотреть»/«уточнить»). Pure-модули — с полным кодом и тестами.

**3. Согласованность типов:** `Session`, `Logger`, `RecursionGuard`, `getRealConstructorConfig`, `screenshotName`/`saveScreenshot`, `launch`/`login`/`close`, `openQueryConsole`, `loadQueryIntoConstructor`/`walkTabs`/`walkSubWindows` — имена и сигнатуры единообразны во всех задачах. ✅
