# Phase 4 — Расширение типов метаданных: дизайн

**Дата:** 2026-06-03  
**Фаза:** 4 (см. [ROADMAP](../../../ROADMAP.md))
**Статус:** утверждён

---

## 1. Цель

Расширить конвейер парсера метаданных и загрузчик YAML, чтобы конструктор запросов
поддерживал все 17 типов объектов метаданных 1С, которые образуют запрашиваемые таблицы:

```
КритерииОтбора, ПланыОбмена, Константы, Справочники, Документы,
Последовательности, ЖурналыДокументов, Перечисления,
ПланыВидовХарактеристик, ПланыСчетов, ПланыВидовРасчета,
РегистрыРасчета, БизнесПроцессы, Задачи,
РегистрыСведений, РегистрыНакопления, РегистрыБухгалтерии
```

Справочники, Документы, Константы и Перечисления уже поддержаны в парсере;
Константы и Перечисления добавляются в загрузчик MetaModel в этой фазе.

Виртуальные таблицы регистров (Остатки, Обороты, Срезы и т.д.) — отдельная задача,
в эту фазу не входят.

---

## 2. Ключевые решения

| Решение | Выбор | Обоснование |
|---|---|---|
| Роль поля регистра | Хранить в `category`: `'dimension' \| 'resource'` | Нужно для будущих виртуальных таблиц; дешевле хранить сейчас |
| Структура кода | Один модуль на тип | Консистентно с текущим паттерном (`catalog.ts`, `document.ts`) |
| Стандартные поля регистров | Условно по свойствам XML | Следует паттерну Catalog (Код только если codeLength > 0 и т.д.) |

---

## 3. Расширение типов данных

### 3.1 `src/core/metadata/parser/model.ts`

Расширить объединение `ParsedObject.kind`:

```ts
kind:
  | 'Справочник' | 'Документ' | 'Константа' | 'Перечисление'   // уже есть
  | 'ПланОбмена' | 'ПланВидовХарактеристик' | 'ПланСчетов' | 'ПланВидовРасчета'
  | 'БизнесПроцесс' | 'Задача'
  | 'РегистрСведений' | 'РегистрНакопления' | 'РегистрБухгалтерии' | 'РегистрРасчета'
  | 'Последовательность' | 'ЖурналДокументов' | 'КритерийОтбора'
```

Расширить `ParsedField.category`:

```ts
category: 'standard' | 'attribute' | 'dimension' | 'resource'
```

### 3.2 `src/core/metadata/types.ts`

Расширить `FieldKind`:

```ts
type FieldKind = 'standard' | 'attribute' | 'dimension' | 'resource'
```

Расширить `TableKind` теми же именами (единственное число, по-русски):

```ts
type TableKind =
  | 'Справочник' | 'Документ' | 'ТабличнаяЧасть'              // уже есть
  | 'Константа' | 'Перечисление'
  | 'ПланОбмена' | 'ПланВидовХарактеристик' | 'ПланСчетов' | 'ПланВидовРасчета'
  | 'БизнесПроцесс' | 'Задача'
  | 'РегистрСведений' | 'РегистрНакопления' | 'РегистрБухгалтерии' | 'РегистрРасчета'
  | 'Последовательность' | 'ЖурналДокументов' | 'КритерийОтбора'
```

---

## 4. Новые модули парсера

Все файлы в `src/core/metadata/parser/`. Каждый экспортирует одну функцию
`parse<Type>(objectEl): ParsedObject | null`.

### 4.1 Catalog-like (есть ТЧ)

Структура XML: `Properties` + `ChildObjects` → `<Attribute>` + `<TabularSection>`.
Парсинг атрибутов и ТЧ делегируется `parseChildObjects` из `attribute.ts`.

#### `exchangePlan.ts` — ПланОбмена

Стандартные поля:

| Поле | Условие |
|---|---|
| Ссылка | всегда |
| ВерсияДанных | всегда |
| ПометкаУдаления | всегда |
| Предопределенный | всегда |
| ИмяПредопределенныхДанных | всегда |
| Код | если `CodeLength > 0` |
| Наименование | если `DescriptionLength > 0` |
| ЭтотУзел | всегда |
| НомерПринятого | всегда |
| НомерОтправленного | всегда |

#### `chartOfCharacteristicTypes.ts` — ПланВидовХарактеристик

Стандартные поля как у Справочника (Ссылка, ВерсияДанных, ПометкаУдаления, Предопределенный,
ИмяПредопределенных, Код(opt), Наименование(opt), ЭтоГруппа(opt), Родитель(opt)) плюс:

| Поле | Условие |
|---|---|
| ТипЗначения | всегда (`ValueType` — стандартный атрибут) |

#### `chartOfAccounts.ts` — ПланСчетов

Стандартные поля как у Справочника плюс:

| Поле | Условие |
|---|---|
| ВидСчета | всегда |
| ПризнакАктивности | всегда |

#### `chartOfCalculationTypes.ts` — ПланВидовРасчета

| Поле | Условие |
|---|---|
| Ссылка | всегда |
| ВерсияДанных | всегда |
| ПометкаУдаления | всегда |
| Предопределенный | всегда |
| ИмяПредопределенныхДанных | всегда |
| Код | если `CodeLength > 0` |
| Наименование | если `DescriptionLength > 0` |

#### `businessProcess.ts` — БизнесПроцесс

| Поле | Условие |
|---|---|
| Ссылка | всегда |
| ВерсияДанных | всегда |
| ПометкаУдаления | всегда |
| Дата | всегда |
| Номер | если `NumberLength > 0` |
| Старт | всегда |
| Завершен | всегда |
| ГоловнаяЗадача | всегда |

#### `task.ts` — Задача

| Поле | Условие |
|---|---|
| Ссылка | всегда |
| ВерсияДанных | всегда |
| ПометкаУдаления | всегда |
| Дата | всегда |
| Номер | если `NumberLength > 0` |
| Наименование | если `DescriptionLength > 0` |
| Выполнена | всегда |
| ТочкаМаршрута | всегда |
| БизнесПроцесс | всегда |

### 4.2 Register-like (нет ТЧ)

Структура XML: `Properties` + `ChildObjects` → `<Dimension>`, `<Resource>`, `<Attribute>`.

Парсинг `<Dimension>` и `<Resource>` — расширить `parseChildObjects` в `attribute.ts`:
добавить `childrenByLocalName(child, 'Dimension')` с `category: 'dimension'` и
`childrenByLocalName(child, 'Resource')` с `category: 'resource'`.
Возвращаемый объект `{ attributes, tabularSections, dimensions, resources }`.

#### `informationRegister.ts` — РегистрСведений

| Поле | Условие |
|---|---|
| НомерСтроки | всегда |
| Активность | всегда |
| Период | если `InformationRegisterPeriodicity != 'Nonperiodical'` |
| Регистратор | если `WriteMode != 'Independent'` |

Дочерние объекты: Dimension + Resource + Attribute.

#### `accumulationRegister.ts` — РегистрНакопления

| Поле | Условие |
|---|---|
| НомерСтроки | всегда |
| Период | всегда |
| Регистратор | всегда |
| ВидДвижения | всегда |

Дочерние объекты: Dimension + Resource.

#### `accountingRegister.ts` — РегистрБухгалтерии

| Поле | Условие |
|---|---|
| НомерСтроки | всегда |
| Период | всегда |
| Регистратор | всегда |
| Активность | всегда |

Дочерние объекты: Dimension + Resource + Attribute.

#### `calculationRegister.ts` — РегистрРасчета

| Поле | Условие |
|---|---|
| НомерСтроки | всегда |
| Период | всегда |
| Регистратор | всегда |
| ВидРасчета | всегда |

Дочерние объекты: Dimension + Resource + Attribute.

### 4.3 Simple

#### `sequence.ts` — Последовательность

| Поле | Условие |
|---|---|
| НомерСтроки | всегда |
| Период | всегда |
| Регистратор | всегда |

Дочерние объекты: только `<Dimension>` → category: `'dimension'`.
Нет Resources, нет Attributes, нет TabularSections.

#### `documentJournal.ts` — ЖурналДокументов

| Поле | Условие |
|---|---|
| Ссылка | всегда |
| ВерсияДанных | всегда |
| Дата | всегда |
| Номер | всегда (составной из разных документов) |
| ТипДокумента | всегда |

Дочерние объекты: `<Column>` → category: `'attribute'`.

#### `filterCriteria.ts` — КритерийОтбора

Только стандартное поле Ссылка. Критерии отбора не имеют собственных атрибутов;
в конструкторе нужны как источник для фильтрации.

---

## 5. Обновление `parseConfiguration.ts`

Добавить 13 строк в массив `HANDLERS`:

```ts
{ subdir: 'ExchangePlans',               parse: parseExchangePlan },
{ subdir: 'ChartsOfCharacteristicTypes', parse: parseChartOfCharacteristicTypes },
{ subdir: 'ChartsOfAccounts',            parse: parseChartOfAccounts },
{ subdir: 'ChartsOfCalculationTypes',    parse: parseChartOfCalculationTypes },
{ subdir: 'BusinessProcesses',           parse: parseBusinessProcess },
{ subdir: 'Tasks',                       parse: parseTask },
{ subdir: 'InformationRegisters',        parse: parseInformationRegister },
{ subdir: 'AccumulationRegisters',       parse: parseAccumulationRegister },
{ subdir: 'AccountingRegisters',         parse: parseAccountingRegister },
{ subdir: 'CalculationRegisters',        parse: parseCalculationRegister },
{ subdir: 'Sequences',                   parse: parseSequence },
{ subdir: 'DocumentJournals',            parse: parseDocumentJournal },
{ subdir: 'FilterCriteria',              parse: parseFilterCriteria },
```

---

## 6. Обновление `yamlLoader.ts`

### 6.1 SUPPORTED_KINDS

Расширить до всех 17 типов (добавить 13 новых + Константа + Перечисление).

### 6.2 mapParsedType

Добавить распознавание ref-ссылок на все новые типы:

```ts
const match = pt.ref.match(
  /^(Справочник|Документ|Константа|Перечисление|ПланОбмена|ПланВидовХарактеристик|ПланСчетов|ПланВидовРасчета|БизнесПроцесс|Задача|РегистрСведений|РегистрНакопления|РегистрБухгалтерии|РегистрРасчета|Последовательность|ЖурналДокументов|КритерийОтбора)\.(.+)$/
);
```

Если тип не распознан — возвращать `{}` (текущее поведение для неизвестных).

### 6.3 parsedObjectToMetaTable

- **Константа**: одно поле `Значение` (kind: `'standard'`) с типом из `obj.types[0]`, нет tabularSections
- **Перечисление**: фиксированные поля Ссылка + Порядок, нет tabularSections
- **Все остальные**: текущая логика (fields + tabularSections)

---

## 7. Тесты

### 7.1 Парсер — по одному тесту на новый модуль

Каждый тест использует реальный XML из `src/cf` (путь от корня проекта).

Для catalog-like:
- name, fullName, kind
- набор стандартных полей (категория `standard`)
- наличие ТЧ и полей ТЧ

Для register-like:
- name, fullName, kind
- условные стандартные поля (Период, Регистратор — проверить и наличие и отсутствие)
- поля из ChildObjects с правильными category: `'dimension'` / `'resource'` / `'attribute'`

Для simple:
- name, fullName, kind
- набор полей

### 7.2 `attribute.ts` — расширение parseChildObjects

Тест что `<Dimension>` → `category: 'dimension'`, `<Resource>` → `category: 'resource'`.

### 7.3 `yamlLoader.ts` — дополнительные тесты

- Загрузка объекта каждого нового типа из синтетического YAML
- Поле с `category: 'dimension'` → `MetaField.kind: 'dimension'`
- Константа с `types` → MetaTable с одним полем
- Перечисление → MetaTable с полями Ссылка + Порядок

---

## 8. Что не входит в фазу 4

- Виртуальные таблицы регистров (Остатки, Обороты, СрезПервых, СрезПоследних, Обороты счетов и т.д.)
- UI конструктора: группировка типов в дереве метаданных — существующий код дерева
  подхватывает новые типы автоматически; специальная группировка (например, папка «Регистры»)
  откладывается
- Обратный разбор (фаза 6)
