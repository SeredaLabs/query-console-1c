# Фаза 8. Сохранение комментариев и доработки

← [Дорожная карта](../ROADMAP.md)

✅ Готово. Опирается на двунаправленное ядро Фаз 6–7 (`parseBatch` → `QueryModel` →
`generateBatch`). Принцип фазы — добавлять возможности **поверх** канонического конвейера,
не меняя его вывод: золотой оракул (`oracleGolden.test.ts`) и регрессия корпуса 1976
запросов (`corpusRegression.test.ts`) обязаны оставаться байт-в-байт. Подфазы: 1 —
сохранение комментариев ✅; 2 — исследование инструмента семантической валидации ✅
(реализация — в подфазе 4); 3 — доработки конструктора (UX/корректность/произв.) ✅;
4 — локальная семантическая валидация открытия ✅.

## Подфаза 1. Сохранение комментариев запроса — ✅ закрыто (2026-06-19)

Спек: [`2026-06-19-phase8.1-comments-design.md`](superpowers/specs/2026-06-19-phase8.1-comments-design.md) ·
план: [`2026-06-19-phase8.1-comments.md`](superpowers/plans/2026-06-19-phase8.1-comments.md).

**Что было не так.** Комментарии `//…` в тексте запроса молча терялись: лексер пропускал
`//` до конца строки, парсер вырезал их из «сырых» срезов (`stripLineComments`). При
сохранении из конструктора текст пересобирался генератором уже без комментариев.

**Итог.** Комментарии сохраняются **по умолчанию**. В шапке (рядом с кнопкой «Обновить
кэш») — галочка «Сохранять комментарии», включена по умолчанию. Комментарии привязываются
к смысловым якорям модели и переживают цикл *открыть → править → сохранить*.

### Правила привязки

Контейнер комментария — запрос пакета: имя ВТ (если запрос `ПОМЕСТИТЬ`), иначе номер N
пакета. Удаление контейнера удаляет его комментарии; переименование/смена типа ВТ — тоже.
Внутри контейнера — 4 вида (всё прочее отбрасывается):

| Вид | Где | Якорь | Удаляется при |
|----|-----|-------|---------------|
| 1 | хвост после поля `Поле КАК Син, // c1` | синоним поля | удаление/переименование поля |
| 2 | отдельной строкой перед полем | синоним следующего поля | удаление/переименование поля |
| 3 | перед `ВЫБРАТЬ` (после авто-разделителя) | `ВЫБРАТЬ` | удаление/переименование контейнера |
| 4 | отдельной строкой после `ИЗ` | `ИЗ` | удаление/переименование контейнера |

Авто-разделитель пакетов `////…` (80 слэшей) генерируется самим `generateBatch` и не
считается комментарием 3-го вида (при извлечении игнорируется).

### Архитектура (где что лежит)

- **Модель** (`queryModel.ts`): `QueryComments { beforeSelect?; afterFrom? }` на
  `QueryModel.comments`; `SelectedField.commentLeading?: string[]` (вид 2) и
  `commentTrailing?: string` (вид 1). Комментарии полей едут на самих полях →
  автоматически переживают перенос и исчезают при удалении поля.
- **Лексер** (`sdblLexer.ts`): тип токена `'comment'`; `tokenize(text, { comments:true })`
  эмитит токены комментариев. По умолчанию — прежнее поведение (комментарии пропускаются).
- **Связыватель** (`commentBinder.ts`, новый): `extractComments(memberText, model)` —
  токенизирует с комментариями, классифицирует по видам 1–4 и мутирует модель. Никогда не
  бросает.
- **Парсер** (`sdblParser.ts`): `parseBatch`/`parseDocument` принимают
  `opts?: ParseOptions { preserveComments? }`; при включении после разбора вызывают
  связыватель на тексте каждого оператора (try/catch). Для **ОБЪЕДИНЕНИЯ** текст режется
  на участников (`splitUnionMemberTexts`, инверсия `splitUnionMembers`), и комментарии
  привязываются к QueryModel каждого участника — т. е. к его **номеру** в объединении.
  По умолчанию — без изменений.
- **Генератор** (`sdblGenerator.ts`): `buildQueryBlock` печатает комментарии из модели
  (beforeSelect перед `ВЫБРАТЬ`; afterFrom между `ИЗ` и телом; leading — отдельными
  строками перед полем; trailing — в хвост строки поля после запятой). Только при наличии
  данных → канонический вывод без комментариев не меняется.
- **Состояние webview** (`queryStore.ts`): `comments` протянут через
  snapshot/restore/model-конвертеры/`assembleBatch`; сброс по правилам в reducer
  (`SET_TEMP_TABLE_NAME`, `SET_QUERY_TYPE`, `SET_COLUMN_ALIAS`, `SET_FIELD_EXPRESSION`);
  хелпер `stripBatchComments` для генерации при снятой галочке.
- **UI/поток** (`App.tsx`, `validateBatch.ts`): `tryParseBatch(text, opts?)`; чекбокс
  «Сохранять комментарии» (по умолчанию вкл); загрузка с `{ preserveComments:true }`,
  вставка — со снятием комментариев при снятой галочке. Протокол сообщений и
  `panel.ts`/`insertResult.ts` не менялись (комментарии уже внутри `batchText`).

### ОБЪЕДИНЕНИЕ

Комментарии извлекаются и для пакетов-ОБЪЕДИНЕНИЙ: текст документа режется на участников
по разделителям `ОБЪЕДИНИТЬ [ВСЕ]` верхнего уровня, и комментарии каждого участника
(виды 1–4) привязываются к его QueryModel — то есть к **номеру участника** в объединении.
Состояние webview хранит участников по позициям (`savedQueries`), поэтому удаление/
переименование участника удаляет его комментарии теми же механизмами, что и для пакета.

### Запрос, начинающийся с комментария (открытие из текста)

1С открывает конструктором запрос, текст которого НАЧИНАЕТСЯ со строки-комментария
(`// …` перед `ВЫБРАТЬ`) — и где хвостовой `//` стоит почти на каждой строке. Наш
экстрактор литерала запроса (`queryAtCursor.findQueryAt`) распознаёт запрос по первому
ключевому слову (`startsWithQueryKeyword`), но снимал только ведущие пробелы — ведущий
комментарий «прятал» `ВЫБРАТЬ`, и литерал не опознавался («Не найден текст запроса» →
конструктор не открывался). Исправлено: `stripLeadingTrivia` снимает ведущие пробелы И
строки-комментарии `//…` перед проверкой ключевого слова. Извлечённый текст по-прежнему
сохраняет ведущий комментарий (он уходит в парсер/связыватель). Хвостовые комментарии на
строках ключевых слов (`ВЫБРАТЬ //…`, `ИЗ //…`, `ПОМЕСТИТЬ Вал //…`) не относятся к 4
видам и отбрасываются (как и предписывает спек).

### Тесты

`sdblLexer.comments`, `commentBinder`, `commentsGenerate`, `commentsRoundTrip` (приёмка:
канонический пример пересобирается байт-в-байт; без опции — без комментариев;
идемпотентность; **ОБЪЕДИНЕНИЕ** — комментарии каждого участника байт-в-байт и привязка к
своему номеру), `queryStore.comments` (правила сброса), `queryAtCursor` (распознавание
запроса с ведущим комментарием). Полный прогон `test:unit` — 1304/1304;
`oracleGolden`/`corpusRegression` — без изменений.

## Подфаза 2. Семантическая валидация текста запроса (MCP 1c-md) — ✅ исследование (2026-06-23)

> Исследование завершено; его рекомендация **реализована** в [Подфазе 4](#подфаза-4-локальная-семантическая-валидация-открытия--закрыто-2026-06-27).

Спек: [`2026-06-23-phase8.2-validation-research-design.md`](superpowers/specs/2026-06-23-phase8.2-validation-research-design.md).
Конфигурация для проверок — живая база `smallb` (MCP `1c-md`). Полный отчёт по 178
проверкам (6 категорий) — ниже, в §§2.A–2.F.

**Что не так.** Критерий открытия конструктора — чисто синтаксический (`tryParseBatch` →
`parseBatch` не бросил). Парсер не знает метаданных, поэтому запрос на **несуществующую
таблицу открывается**. Эталон из требований не должен открываться, но открывается:

```
ВЫБРАТЬ Валюты.Ссылка КАК Ссылка, Валюты.Наименование КАК Наименование
ИЗ Справочник.Валюты1 КАК Валюты
  ЛЕВОЕ СОЕДИНЕНИЕ Справочник.БанковскиеСчета КАК БанковскиеСчета
  ПО Валюты.ОсновнаяВалюта = БанковскиеСчета.Ссылка
```

(`Справочник.Валюты1` не существует — есть `Справочник.Валюты`).

**Исследование.** Прежде чем менять критерий, охарактеризовали инструмент MCP
`1c-md.validate_query` на **178 проверках** (6 параллельных субагентов, живая база
`smallb`): таблицы, поля/навигация, синтаксис, виртуальные таблицы регистров, сложные
конструкции, реальный корпус `tmp/query1c/` с разметкой `{…}`.

**Итог.** `validate_query` — это **полноценный парсер 1С + сверка с метаданными** (а не
только синтаксис, вопреки описанию). Эталонный «плохой» текст корректно **не открывается**
(`{(5,2)}: Таблица не найдена "Справочник.Валюты1"` + маркер `<<?>>`), исправленный —
открывается.

- ✅ **Проверяет:** существование таблиц/полей/ТЧ, навигацию через точку (только по
  ссылочным), соответствие префикса типа, арность ОБЪЕДИНЕНИЯ, согласованность
  группировки, значения `ЗНАЧЕНИЕ(Перечисление.…)`, применимость виртуальной таблицы к
  типу регистра и состав её параметров, поля ВТ (суффиксы Остаток/Оборот), колонки
  *существующей* ВТ между пакетами, дубль псевдонима. Всегда отдаёт позицию
  `{(строка,столбец)}`.
- ⚠️ **Не проверяет:** существование ВТ по «голому» имени без точки (CTE-семантика), тип
  значений `&П` и параметров ВТ, логику отбора по ресурсу среза.
- ⚠️ **Не поддерживает** скалярный подзапрос `(ВЫБРАТЬ …)` в списке полей.
- ℹ️ Регистронезависим; принимает английский синтаксис и префиксы `Catalog.`/`Document.`;
  при успехе нормализует текст. **Конфигураторную разметку `{…}` принимает как есть** —
  вырезать НЕ нужно, достаточно снять ведущие `|`.

**Рекомендация (отдельной подфазой реализации).** MCP `1c-md` — **только инструмент
исследования, в рантайме расширения не используется ни в коем случае** (никакого вызова
`validate_query` при открытии/«ОК» — расширение не должно зависеть от внешнего сервиса).
Семантическую проверку (существование таблиц/полей) делать **локально по кэшу метаданных**
(Фаза 7.2), который у конструктора уже есть. Исследование лишь показало эталон поведения,
которое нужно воспроизвести локально: какие тексты «открываются», какие — нет, и какие
именно сообщения об ошибке давать. Реализация — за рамками исследования.

Эталонные границы (что воспроизводить, а что — нет): голое имя без точки трактуем как
ВТ/подзапрос и не проверяем; типы значений `&П` и параметров ВТ не проверяем; разметку
`{…}` обрабатываем прозрачно (она не влияет на состав таблиц/полей). Чувствительность к
регистру — нет; английские префиксы — по желанию.

### Эталонный пример — прямая проверка

| Текст | valid | Сообщение |
|-------|-------|-----------|
| `…ИЗ Справочник.Валюты1 КАК Валюты…` (как в требовании) | ❌ false | `{(5, 2)}: Таблица не найдена "Справочник.Валюты1"` + маркер `<<?>>` |
| тот же запрос, но `Справочник.Валюты` | ✅ true | Синтаксис запроса корректен |

→ инструмент решает задачу: эталонный «плохой» текст **не открывается**, исправленный — открывается.
**«Открывается»** == `{valid:true}`; **«не открывается»** == `{valid:false}`.

#### Нюансы нормализации (на validity не влияют)

- Регистронезависимость имён и ключевых слов; поле без префикса при одной таблице авто-дополняется.
- Английский синтаксис `SELECT…AS…FROM` и английские префиксы `Catalog.`/`Document.` принимаются и переводятся в русские.
- **Конфигураторная разметка `{…}`** (`{(&Параметр)}`, `{ГДЕ …}`, `{ВЫБРАТЬ …}`, измерения `{(Поле)}`) **принимается как есть и нормализуется** — удалять её перед валидацией НЕ нужно.
- Пустой ввод, только пробелы, только `//`-комментарий — невалидны (нужен `ВЫБРАТЬ`); ровно один хвостовой `;` допустим, несколько подряд или ведущий — нет.

### 2.A. Существование таблиц (30 проверок)

| # | Вариант | valid | Сообщение |
|---|---|---|---|
| 1 | Справочник.АвтоматическиеСкидки (.Ссылка) | ✅ | корректен |
| 2 | Документ.АвансовыйОтчет (.Ссылка) | ✅ | корректен |
| 5 | РегистрНакопления.БонусныеБаллы (.Регистратор) | ✅ | корректен |
| 6 | РегистрБухгалтерии.Управленческий (.Регистратор) | ✅ | корректен |
| 7 | Перечисление.АналитикаПлатежногоКалендаря (.Ссылка) | ✅ | корректен |
| 8 | ПланСчетов.Управленческий (.Ссылка) | ✅ | корректен |
| 9 | ПланВидовХарактеристик.НастройкиПользователей (.Ссылка) | ✅ | корректен |
| 10 | БизнесПроцесс.Задание (.Ссылка) | ✅ | корректен |
| 11 | Задача.ЗадачаИсполнителя (.Ссылка) | ✅ | корректен |
| 12 | ПланОбмена.АвтономнаяРабота (.Ссылка) | ✅ | корректен |
| 13 | Константы (виртуальная) .ИмяКонстанты | ✅ | корректен |
| 14 | Константа.X (.Значение) | ✅ | корректен |
| 15 | Константа.X (.X — поля-имени нет) | ❌ | Поле не найдено "Т.X" |
| 16 | **Справочник.Валюты1** (опечатка) | ❌ | `(1,21)` Таблица не найдена "Справочник.Валюты1" |
| 17 | Документ.АвтоматическиеСкидки (неверный префикс, это справочник) | ❌ | `(1,21)` Таблица не найдена |
| 18 | Catalog.АвтоматическиеСкидки (англ. префикс существующего) | ✅ | корректен |
| 19 | Document.АвансовыйОтчет (англ. префикс существующего) | ✅ | корректен |
| 20 | Catalog.НесуществующийТакой | ❌ | `(1,21)` Таблица не найдена |
| 21 | Справочник.ВыдуманныйСправочник | ❌ | `(1,21)` Таблица не найдена |
| 22–23 | Выдуманный регистр / перечисление | ❌ | Таблица не найдена |
| 24 | `ИЗ ВыдуманныйТипБезТочки` (имя без точки) | ✅ | корректен — трактуется как ВТ/подзапрос |
| 25 | `ИЗ Справочник` (голый тип) | ✅ | корректен — как ВТ |
| 26–27 | регистр символов (нижний / ВЕРХНИЙ) | ✅ | корректен — регистронезависимо |
| 28 | `ИЗ КАК Т` (пустое имя) | ❌ | `(1,22)` Неверное присоединение |

**Вывод A:** существование таблицы с точкой проверяется строго и семантически; префикс
типа должен соответствовать объекту; язык префикса и регистр символов не важны; «голое»
имя без точки существование НЕ проверяется (CTE-семантика).

### 2.B. Поля, реквизиты, ТЧ, навигация (24 проверки)

| # | Вариант | valid | Сообщение |
|---|---|---|---|
| 1 | Стандартные: Ссылка/Код/Наименование/ПометкаУдаления/Предопределённый/ЭтоГруппа | ✅ | корректен |
| 3 | К.НесуществующееПоле | ❌ | `(1,11)` Поле не найдено |
| 4 | ИЗ Справочник.Контрагенты.КонтактнаяИнформация (ТЧ как таблица) | ✅ | корректен |
| 5 | …НесуществующаяТЧ | ❌ | `(1,21)` Таблица не найдена |
| 7 | Несуществующее поле ТЧ | ❌ | `(1,11)` Поле не найдено |
| 8 | К.Ответственный.Наименование (навигация по ссылке) | ✅ | корректен |
| 9 | К.Ответственный.НесуществующееПоле | ❌ | `(1,25)` Поле не найдено |
| 10 | К.ГоловнойКонтрагент.Родитель.Наименование (2 точки) | ✅ | корректен |
| 11 | Ч.Наименование при `КАК К` (неверный псевдоним) | ❌ | `(1,9)` Поле не найдено |
| 13 | `КАК Поле1 … КАК Поле1` (дубль псевдонима) | ❌ | `(1,35)` Повторяющийся псевдоним "Поле1" |
| 14 | `Наименование` без префикса при одной таблице | ✅ | авто-дополнено до К.Наименование |
| 15–16 | регистр поля/навигации (строчные/ВЕРХНИЕ) | ✅ | нормализуется |
| 18 | К.ВидКонтрагента.Представление (навигация по перечислению) | ❌ | `(1,26)` Поле не найдено |
| 21 | К.Поставщик(Булево).Наименование (навигация по примитиву) | ❌ | `(1,21)` Поле не найдено |
| 22 | `КАК ВЫБРАТЬ` (зарезервированное слово как псевдоним) | ✅ | принято |

**Вывод B:** поля проверяются на существование; навигация через точку разыменовывается
по типу реквизита (только ссылочные); дубль псевдонима ловится; при успехе текст
нормализуется (авто-псевдонимы `КАК <Поле>`).

### 2.C. Синтаксис и пограничные случаи (30 проверок)

| # | Вариант | valid | Сообщение |
|---|---|---|---|
| 1 | Без `ВЫБРАТЬ` | ❌ | `(1,1)` Ожидается выражение "ВЫБРАТЬ" |
| 3 | `ВЫБРАТЬ 1 КАК Один` (константа без ИЗ) | ✅ | корректен |
| 4–5 | `ВЫБРАТЬ "текст"` / `ВЫБРАТЬ ДАТАВРЕМЯ(2020,1,1)` | ✅ | авто-алиас Поле1 |
| 6 | Несбалансированные скобки | ❌ | `(1,16)` Ожидается ")" |
| 7 | Двойная запятая | ❌ | `(1,18)` Синтаксическая ошибка "," |
| 8 | Пропущена запятая | ❌ | `(1,17)` Синтаксическая ошибка |
| 9 | `ПО` без СОЕДИНЕНИЕ | ❌ | `(1,57)` Синтаксическая ошибка "ПО" |
| 11–13 | Пустая строка / только пробелы / только `//` | ❌ | Ожидается "ВЫБРАТЬ" |
| 14 | `ВЫБРАТЬ *` с таблицей | ✅ | раскрылся во все поля |
| 15 | `ВЫБРАТЬ *` без таблицы | ✅ | query_text пустой |
| 16–17 | один хвостовой `;` | ✅ | корректен |
| 18–19 | `;;;` подряд / ведущая `;` | ❌ | Ожидается "ВЫБРАТЬ" |
| 20 | Незакрытая кавычка | ❌ | `(1,9)` Строка, не закрывающаяся кавычкой |
| 21–22 | Опечатки `ВЫБРАT` / `ИЗЪ` | ❌ | синтакс. ошибка |
| 23 | строчные ключевые слова | ✅ | нормализуется |
| 24–25 | `SELECT 1 AS One` / `SELECT … FROM Catalog.…` | ✅ / ❌ | англ. синтаксис принят; падает только на метаданных |
| 27–28 | комментарии `//` / лишние пробелы и табы | ✅ | игнорируются |
| 29 | Висячая запятая в конце списка | ❌ | синтакс. ошибка |

**Вывод C:** английский синтаксис и регистр ключевых слов поддержаны; пустой/комментарный
ввод невалиден; синтаксические дефекты ловятся с точной позицией.

### 2.D. Виртуальные таблицы регистров (30 проверок)

Реальные регистры: `РегистрНакопления.ЗапасыНаСкладах` (остаточный),
`РегистрНакопления.Продажи` (оборотный), `РегистрСведений.ЦеныНоменклатуры` (периодический),
`РегистрБухгалтерии.Управленческий`.

| # | Вариант | valid | Сообщение |
|---|---|---|---|
| 1 | Накопл .Остатки, поле `Количество` (без суффикса) | ❌ | Поле не найдено "Количество" |
| 2 | Накопл .Остатки(&Дата, ), поле `КоличествоОстаток` | ✅ | корректен |
| 3 | Накопл .Обороты(нач, кон, Месяц, ) | ✅ | корректен |
| 4 | Накопл .ОстаткиИОбороты(…, ДвиженияИГраницыПериода, ) | ✅ | корректен |
| 5–6 | РС .СрезПоследних / .СрезПервых(&Дата, ВидЦен=&ВидЦен) | ✅ | корректен |
| 7–8 | РС .СрезПоследних() / без скобок | ✅ | нормализуется |
| 13 | СрезПоследних у регистра **накопления** | ❌ | Таблица не найдена "…СрезПоследних" |
| 14 | .Остатки у **оборотного** Продажи | ❌ | Таблица не найдена "Продажи.Остатки" |
| 15 | .Обороты у оборотного Продажи | ✅ | корректен |
| 19 | Несуществующий ресурс `СуммаОстаток` | ❌ | Поле не найдено |
| 20 | Условие по несуществующему полю `Картошка` | ❌ | Поле не найдено |
| 22 | Лишний параметр ВТ | ❌ | Неверные параметры |
| 23 | Недопустимая периодичность | ❌ | Неверные параметры |
| 24 | Число вместо даты в параметре | ✅ | корректен — тип не проверяется |
| 28 | .Обороты у регистра **сведений** | ❌ | Таблица не найдена |
| 30 | .ОстаткиИОбороты у оборотного Продажи | ❌ | Таблица не найдена |

**Вывод D:** применимость ВТ к типу регистра и состав параметров проверяются; поля ВТ —
по фактической структуре (суффиксы Остаток/Оборот); тип значений параметров — нет.

### 2.E. Сложные конструкции (38 проверок)

| # | Вариант | valid | Сообщение |
|---|---|---|---|
| 1 | ОБЪЕДИНИТЬ ВСЕ, 2=2 колонки | ✅ | корректен |
| 2 | ОБЪЕДИНИТЬ, 2 vs 3 колонки | ❌ | Разное количество полей в объединяемых запросах |
| 3–4 | Вложенный запрос в ИЗ (валидный / с несущ. полем) | ✅ / ❌ | Поле не найдено внутри |
| 5,35 | Скалярный подзапрос `(ВЫБРАТЬ …)` в списке полей | ❌ | Синтаксическая ошибка "ВЫБРАТЬ" |
| 6 | Пакет `ПОМЕСТИТЬ ВТ` → `ВЫБРАТЬ ИЗ ВТ` | ✅ | корректен |
| 7 | 2-й пакет: невыбранное поле существующей ВТ | ❌ | Поле не найдено |
| 8 | УНИЧТОЖИТЬ ВТ | ✅ | корректен |
| 9–12 | ЛЕВОЕ/ПРАВОЕ/ВНУТРЕННЕЕ/ПОЛНОЕ СОЕДИНЕНИЕ … ПО | ✅ | корректен |
| 13 | СОЕДИНЕНИЕ ПО несущ. полю | ❌ | Поле не найдено |
| 14 | СГРУППИРОВАТЬ + СУММА/КОЛИЧЕСТВО/МАКС/МИН/СРЕДНЕЕ | ✅ | корректен |
| 15 | Поле вне группировки/агрегата | ❌ | Поле не входит в группу |
| 16–17 | ИМЕЮЩИЕ / ИТОГИ … ПО | ✅ | корректен |
| 18,36 | ВЫБОР КОГДА … (с ИНАЧЕ и без) | ✅ | корректен |
| 19–20 | ВЫРАЗИТЬ(… КАК СТРОКА(50)) / приведение ссылки `.Реквизит` | ✅ | корректен |
| 21–22 | ЕСТЬNULL/ПОДСТРОКА / ДАТАВРЕМЯ/НАЧАЛОПЕРИОДА | ✅ | корректен |
| 23 | ЗНАЧЕНИЕ(Перечисление.X.ВалидноеЗначение) | ✅ | корректен |
| 24 | ЗНАЧЕНИЕ(Перечисление.X.Несуществующее) | ❌ | Неверные параметры |
| 25 | ЗНАЧЕНИЕ(Справочник.X.ПустаяСсылка) | ✅ | корректен |
| 26–30 | &Параметр / `В (…)` / `В ИЕРАРХИИ` / `МЕЖДУ` / `ССЫЛКА` | ✅ | корректен |
| 31–33 | РАЗЛИЧНЫЕ / ПЕРВЫЕ N + УПОРЯДОЧИТЬ + АВТОУПОРЯДОЧИВАНИЕ / ДЛЯ ИЗМЕНЕНИЯ | ✅ | корректен |
| 37 | 2-й пакет: `ИЗ ДругаяВТ` (несуществующая ВТ, голое имя) | ✅ | корректен — существование ВТ не сверяется |

**Вывод E:** проверяются арность ОБЪЕДИНЕНИЯ, согласованность группировки, значения
перечислений, поля в подзапросах и соединениях. Скалярный подзапрос в колонке не
поддержан. **Существование ВТ по голому имени между пакетами не проверяется** (ср. с
категорией F, где колонки *существующей* ВТ всё же сверяются).

### 2.F. Реальный корпус `tmp/query1c/` + разметка `{…}` (18 проверок)

Выбраны 8 реальных файлов конфигурации (от 18 до 1754 строк, до 23 пакетов, с разметкой `{…}`).

| # | Файл/вариант | пакетов/строк | valid | Сообщение |
|---|---|---|---|---|
| 1 | F3 как есть (`{(&НачалоПериода)}`, `{ГДЕ}`) | 1 / 18 | ✅ | корректен |
| 2 | F3 очищенный (без `{…}`) | 1 / 16 | ✅ | корректен |
| 3 | F3 поломка: `РегистрНакопления.ОплатаСБП1` | 1 | ❌ | `{(8,2)}` Таблица не найдена |
| 4–5 | F4 как есть / очищенный (`{ВЫБРАТЬ …}`) | 2 | ✅ | корректен |
| 6–7 | F1 1-й пакет как есть / очищенный (`{ГДЕ поле.*}`) | 1 | ✅ | корректен |
| 8 | F2 урезанный (потеря колонки ВТ `ВидЗанятости`) | 2 | ❌ | `{(22,35)}` Поле не найдено |
| 9 | F2 полный (все колонки ВТ) | 2 | ✅ | корректен |
| 10 | F6 многопакетный как есть (`{ГДЕ Шапка.Ссылка.*}`) | 4 | ✅ | корректен |
| 11 | F6 поломка: `…СоответствиеНоменклатурыВЕТИС1` | 1 | ❌ | `{(6,2)}` Таблица не найдена |
| 13 | F5 вложенные подзапросы + `{ГДЕ … В (&Орг)}` | 1 / 265 | ✅ | корректен |
| 14 | F8 крупный кусок (.ОстаткиИОбороты с `{…}` в измерениях, ОБЪЕДИНИТЬ, УНИЧТОЖИТЬ) | 7 | ✅ | корректен |
| 16 | F1 поломка: `…ОтработанноеВремяПоСотрудникам1` | 1 | ❌ | `{(7,2)}` Таблица не найдена |
| 17 | F4 поломка: `…РазмерОбъектовМетаданныхПриложений1.СрезПоследних` | 1 | ❌ | `{(5,2)}` Таблица не найдена |

**Вывод F:**
1. **Разметка `{…}` принимается КАК ЕСТЬ и нормализуется** — вырезать её перед валидацией
   НЕ нужно (валидны `{(&Параметр)}` в параметрах ВТ, `{ГДЕ поле.*}`, `{ВЫБРАТЬ …}`,
   измерения `{(Поле)}`); очищенные варианты тоже валидны.
2. Реальные сложные многопакетные запросы (до 23 пакетов, ПОМЕСТИТЬ/ИНДЕКСИРОВАТЬ/УНИЧТОЖИТЬ,
   ОБЪЕДИНИТЬ, глубоко вложенные подзапросы, ВТ с период-измерениями) — валидны.
3. Колонки **существующей** временной таблицы сверяются между пакетами (обращение к
   невыбранной колонке → «Поле не найдено», #8).
4. Позиция ошибки указывается и в крупных запросах; координаты `{(строка,столбец)}` —
   от начала текущего пакета.
5. Достаточно убрать ведущие `|` (в этих файлах их и так нет); падение валидации почти
   всегда = реальное расхождение с метаданными, а не синтаксис/разметка.

## Подфаза 3. Доработки конструктора (UX + корректность + производительность) — ✅ закрыто (2026-06-23)

Спек: [`2026-06-23-phase8.3-constructor-ux-design.md`](superpowers/specs/2026-06-23-phase8.3-constructor-ux-design.md).
Источник: ручное тестирование на реальных запросах (`tmp/1.bsl`, эталон ИТОГИ
`tmp/1с/Снимок экрана_20260623_211010.png`). Все доработки — поверх канонического
конвейера; `test:unit` 1304/1304, золотой оракул/корпус-регрессия — байт-в-байт.

| № | Что | Где |
|---|-----|-----|
| 8.3.1 | Кнопки ↑/↓ порядка колонок объединения. Колонки позиционны → новое действие `MOVE_UNION_COLUMN { index, dir }` свопит i-е скалярное поле во **всех** участниках | `UnionsTab.tsx`, `queryStore.ts`, `ConstructorView.tsx` |
| 8.3.2 | «Итоги»: правильное заполнение (подпись из `operandAlias`), перетаскивание **любого** поля (снят фильтр «числовые»), «Выражение» → выпадающий список функций (Количество / Количество различные / Максимум / Минимум). Адресация итоговых полей — по **индексу** (`REMOVE_TOTAL_FIELD`, `SET_TOTAL_FIELD_FUNC`) | `TotalsTab.tsx`, `queryStore.ts`, `ConstructorView.tsx` |
| 8.3.3 | Убраны подсказки «Перетащите …» (Условия/Порядок/Построитель); «Нет полей…» сохранена | `ConditionsTab.tsx`, `OrderTab.tsx`, `BuilderTab.tsx` |
| 8.3.4 | Список «Запросы пакета» растянут на всё пространство (`flex:1` вместо `width:320`) | `BatchTab.tsx` |
| 8.3.5 | Поля-выражения показываются **началом текста выражения** (`ВЫБОР КОГДА …`), а не синонимом (он при сборке ещё неизвестен) | `FieldsPanel.tsx` |
| 8.3.6 | Производительность: удалён неиспользуемый `batchText` в `ConstructorView` (полная генерация пакета впустую на каждом ре-рендере); `useMemo` для `assembleMembers`/`deriveUnionColumns`/`batchNames`/`nestedBatchText` и для `batchText` в `App` | `ConstructorView.tsx`, `App.tsx` |
| 8.3.7 | Перетаскиваемые границы панелей/колонок: новый `ResizeHandle`, применён к окну «Произвольное выражение» и вкладкам (Таблицы и поля, Условия, Порядок, Итоги, Построитель, Объединения/Псевдонимы, Группировка, Индексы) | `ResizeHandle.tsx` (новый) + вкладки |

## Подфаза 4. Локальная семантическая валидация открытия — ✅ закрыто (2026-06-27)

Спек: [`2026-06-27-phase8.4-local-semantic-validation-design.md`](superpowers/specs/2026-06-27-phase8.4-local-semantic-validation-design.md) ·
план: [`2026-06-27-phase8.4-local-semantic-validation.md`](superpowers/plans/2026-06-27-phase8.4-local-semantic-validation.md).

**Что было не так.** Критерий открытия конструктора — чисто синтаксический (`tryParseBatch`
не бросил), поэтому запрос на **несуществующую таблицу** открывался. Эталон из требований
(`ИЗ Справочник.Валюты1`) должен НЕ открываться, но открывался.

**Итог.** Реализована рекомендация исследования 8.2: семантическая проверка делается
**локально по кэшу метаданных** (Фаза 7.2); MCP `validate_query` в рантайме не вызывается.
Эталонный «плохой» текст больше не открывается (`{(строка, столбец)}: Таблица не найдена
"Справочник.Валюты1"`), исправленный — открывается.

### Объём и границы

В объёме: **существование таблицы-источника** по полному имени `Тип.Имя` (реальные объекты,
виртуальные таблицы регистров через `virtualTableByFullName`, табличные части через
`canonicalFullName`) и **повтор явного псевдонима** поля выборки. Поля/реквизиты/навигация
**не проверяются**. Стандартные поля (`Ссылка`/`Код`/…) уже присутствуют в
`MetaTable.fields`; причина исключения поля из текущего периметра другая: для
повної перевірки потрібно коректно розв'язувати табличні частини, віртуальні
таблиці, тимчасові таблиці, псевдоніми та навігацію без хибних спрацювань. Це
винесено в задел (спек §6).

Ведущий принцип — **отсутствие ложных срабатываний** (отказ открыть валидный запрос хуже
пропуска невалидного):
- **fail-open**: при пустом/отсутствующем кэше резолвер не строится → проверка пропускается;
- проверяются только источники с точкой, чей первый сегмент ∈ фиксированного набора видов
  (`TYPE_PREFIXES`); голые имена (ВТ/CTE), подзапросы-источники, `КритерийОтбора.…` — пропуск;
- **3-сегментные подтаблицы** проверяются лишь для видов, чьи срезы/ТЧ реально материализуются
  в кэше (`SUBTABLE_CHECKED_TYPES` = Справочник/Документ/РегистрНакопления/РегистрСведений/
  РегистрБухгалтерии) и только при резолвящейся 2-сегментной базе. Подтаблицы
  РегистрРасчета (`ДанныеГрафика` и пр.) и `ТочкаМаршрута` БизнесПроцесса/Задачи в кэш не
  попадают → по ним fail-open (не блокируем валидный запрос). Найдено адверсариальным ревью.

### Архитектура (где что лежит)

- **`src/core/query/semanticValidator.ts`** (новый): `validateBatchSemantics(doc, resolver,
  text)` → `SemanticError[]`. Рекурсивный обход модели пакета (участники ОБЪЕДИНЕНИЯ,
  подзапросы-источники, подзапросы условий/`ИМЕЮЩИЕ`). Сообщение в стиле эталона; позиция
  `{(строка, столбец)}` — best-effort по токенам (`findPosition`, не бросает).
- **`src/core/metadata/buildModelResolver.ts`** (новый): `buildResolverFromTables(tables)` —
  in-memory `MetadataResolver` из `MetaTable[]` (три карты: реальные/виртуальные/канонические).
  `buildYamlResolver` отрефакторен в делегата (единый источник правды).
- **`src/core/query/validateBatch.ts`**: `tryOpenBatch(text, resolver?, opts?)` (синтаксис +
  семантика) и необязательный `resolver?` у `validateBatchText`. Без резолвера — прежнее
  синтаксис-only поведение (обратная совместимость).
- **`src/webview/App.tsx`**: `metaTablesRef` ← `metadataTree`; резолвер из неё (только при
  непустых таблицах); открытие из текста → `tryOpenBatch`; «ОК» → `validateBatchText(.., resolver)`;
  оверлей `load-error` показывает и семантическую ошибку (заголовок «Не удалось открыть запрос»).

### Тесты

`semanticValidator` (эталонный пример +/−, неверный префикс типа, неприменимый срез регистра,
валидный срез, ТЧ-источник, голое имя, вложенные подзапросы, регистронезависимость, повтор
псевдонима, fail-open, 3-сегментные подтаблицы РР/БП/Задачи без ложных срабатываний),
`buildModelResolver`, дополнения `validateBatch`. Полный прогон `test:unit` — **1336/1336**;
`oracleGolden`/`corpusRegression` — байт-в-байт; `tsc`/`build` чисты.
