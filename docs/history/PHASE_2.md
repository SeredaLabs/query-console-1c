# Фаза 2. Конструктор читает YAML

← [Дорожная карта](pre-consolidation/ROADMAP.md)

✅ Готово.

Перевести слой метаданных конструктора на чтение YAML-артефакта парсера (или кэша из
него) вместо текущего пути `cfParser.ts` → `cacheBuilder`/`cacheLoader`. Старый путь
пока остаётся рабочим.
